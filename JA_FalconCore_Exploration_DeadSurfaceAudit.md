# JA FalconCore — استكشاف: حصر المدخلات والتقارير الميّتة (Dead Surface Audit)
## مهمّة استكشاف (قراءة فقط)

> **مهمّة استكشاف فقط — لا تعديل، لا حذف، لا كود، لا أوامر
> bash/git تكتب شيئًا. تُشغَّل في Plan Mode.** تحليل كود بحت.
> أخرِج التقرير في ردّك. لا بناء، لا تغيير `EA_VERSION_TAG`. لا
> تمسّ ترتيب سلسلة الـ Apply*.
> **الأساس:** الشجرة الحاليّة بعد إغلاق 3a (commit محرّك الحماية).

---

## السياق

تراكمت عبر مراحل المشروع «سقالات» (scaffolding): مدخلات تشخيصيّة
أُضيفت لمرحلةٍ ثمّ انتهى غرضها، وتقارير CSV تشخيصيّة خدمت تحقيقًا
بعينه ولم تعد تُقرأ. تركها يضخّم شاشة الإعدادات ويشوّش المخرجات.

نريد مرحلة تنظيف — لكنّ **الحذف بالحدس خطر**: ثبت في هذا المشروع
مرارًا أن ما يبدو «مؤقّتًا» له قارئٌ خفيّ. لذا، قبل أيّ حذف، نحصر
بالدليل — grep لكلّ موضع قراءة — ما هو **ميتٌ فعلًا** وما هو حيّ.
هذا الاستكشاف لا يحذف شيئًا؛ يُنتج خريطة قرار تُبنى عليها مواصفة
التنظيف لاحقًا.

## الهدف

إنتاج جردٍ مدعومٍ بالكود (`المسار:السطر`) لكلّ:
1. **مدخل `input`** في المشروع — مصنّفًا: حيّ / ثابت غير مضبوط /
   ميت.
2. **تقرير CSV تشخيصيّ** يكتبه الـ EA — مصنّفًا: مستعمَل / منتهي
   الغرض.

دون اقتراح كود ودون حذف — تشخيصٌ فقط.

## محاور الاستكشاف

### المحور ١ — جرد كلّ مدخلات `input`
استخرج كلّ سطر `input` في المشروع (الـ `.mq5` الرئيسيّ + كلّ
`*Inputs.mqh` + أيّ ملفّ آخر). لكلّ مدخل، أعطِ: الاسم، الملفّ
والسطر، التعليق الظاهر، والقيمة الافتراضيّة.

### المحور ٢ — تصنيف كلّ مدخل بالدليل
لكلّ مدخل، ابحث (grep) عن **كلّ مواضع قراءته** في الكود، وصنّفه:

- **حيّ** — يُقرأ في منطقٍ يؤثّر على السلوك (شرط، حساب، تفعيل).
  اذكر موضعَ قراءةٍ واحدًا مثالًا.
- **ثابتٌ غير مضبوط** — يُقرأ، لكنّه قيمةٌ معياريّة لا يضبطها
  المشغّل أبدًا (نمط `AtrPeriod=14`). مرشّحٌ للتحويل إلى `#define`
  — يختفي من شاشة الإعدادات، يبقى سلوكه.
- **ميت** — **لا قارئ له إطلاقًا** في الكود (عدا تعريفه). مرشّحٌ
  للحذف الكامل.
- **تشخيصيّ منتهٍ** — مدخلٌ يُفعّل تقريرًا/سلوكًا تشخيصيًّا خدم
  مرحلةً انتهت (مثل مفاتيح `*_DiagReport` لمراحل مُغلقة). يُقرأ
  فعلًا، لكنّ غرضه انقضى. ميِّزه عن «الحيّ».

**حذرٌ واجب:** قبل وسم أيّ مدخل «ميت»، تأكّد أنه لا يُقرأ عبر اسمٍ
غير مباشر، ولا في سلسلة نصّيّة، ولا في ملفّ `.set`. الشكّ ⇒ صنّفه
«حيّ» لا «ميت».

### المحور ٣ — جرد التقارير التشخيصيّة
احصر كلّ تقرير CSV يكتبه الـ EA (عبر `FalconBuildReportFileName`
أو ما يماثله). لكلّ تقرير: اسمه، الكود الذي يكتبه، والمفتاح/المدخل
الذي يُفعّله إن وُجد.

### المحور ٤ — تصنيف كلّ تقرير
لكلّ تقرير، حدّد:
- **مستعمَل/جوهريّ** — جزءٌ من التحقّق الجاري (الستّة التي نرفعها:
  `Summary`, `TradeLifecycle`, `BrokerExecutionLifecycle`,
  `BrokerPaperTradeReconciliation`, `TradeManagementCoordinatorAudit`).
- **تشخيصيّ منتهي الغرض** — أُنشئ لتحقيقٍ بعينه (مرحلة مُغلقة) ولم
  يعد يُقرأ ولا يُحلَّل. مرشّحٌ لإطفاءٍ افتراضيّ أو حذف.
- لاحظ تحديدًا التقارير التي ظهرت في حِزَم الاختبار دون أن نستعملها
  في التحليل (مثل `BrokerContractRealityAudit`,
  `BrokerRebasedPaperComparison`, `BrokerTradeManagementEventTimeline`,
  `S00_FvgDetection_Diagnostics`) — هل ما زال لها قارئٌ/غرضٌ، أم
  انتهت؟

### المحور ٥ — المخاطر والترابط
- أيّ مدخل «ميت» ظاهريًّا لكنّه يظهر في ملفّات `.set` المحفوظة
  (حذفه يكسر تحميل `.set` قديم)؟
- أيّ تقرير «منتهٍ» يقرؤه تقريرٌ آخر أو فحصُ نزاهة؟
- أيّ مدخل تشخيصيّ يرتبط إطفاؤه بتعطيل تقريرٍ نحتاجه؟

## صيغة الإخراج

تقرير منظّم. في صلبه **جدولان**:

**جدول المدخلات** — عمود لكلّ: الاسم، `المسار:السطر`، التصنيف
(حيّ / ثابت-غير-مضبوط / ميت / تشخيصيّ-منتهٍ)، موضع قراءةٍ مثال،
والإجراء المقترَح (إبقاء / `#define` / حذف / إطفاء افتراضيّ).

**جدول التقارير** — عمود لكلّ: اسم التقرير، الكود الكاتب،
المفتاح المُفعِّل، التصنيف (جوهريّ / منتهٍ)، والإجراء المقترَح.

ثمّ:
- **ملخّص:** كم مدخلًا حيٌّ / ثابت / ميت / تشخيصيّ-منتهٍ؛ كم تقريرًا
  جوهريٌّ / منتهٍ.
- **قائمة المخاطر** من المحور ٥ — كلّ ترابطٍ يجب أن تحترسه مواصفةُ
  التنظيف.
- أيّ غموض — أيّ مدخل/تقرير لم يُحسَم تصنيفه، ولماذا.

**لا تقترح كودًا ولا تحذف شيئًا** — هذا جردٌ تشخيصيّ. على ضوئه
تُكتب مواصفة التنظيف.

---

## ملحق — Prompt جاهز لـ Claude Code

> فعّل وضع Plan Mode أوّلًا — مهمّة استكشاف قراءة-فقط، لا تعدّل
> ولا تحذف أيّ ملفّ، لا بناء، لا git، لا تغيير `EA_VERSION_TAG`،
> لا تمسّ ترتيب سلسلة الـ Apply*. اقرأ
> `JA_FalconCore_Exploration_DeadSurfaceAudit.md` ونفّذ الاستكشاف.
> الهدف: جردٌ مدعومٌ بالكود لكلّ مدخلات `input` وكلّ تقارير الـ CSV
> التشخيصيّة. (١) استخرج كلّ سطر `input` (الـ `.mq5` + ملفّات
> `*Inputs.mqh` + غيرها) مع `المسار:السطر` والتعليق والقيمة. (٢)
> لكلّ مدخل، grep لكلّ مواضع قراءته وصنّفه: حيّ / ثابت-غير-مضبوط
> (مرشّح `#define`) / ميت (لا قارئ — مرشّح حذف) / تشخيصيّ-منتهٍ.
> قبل وسم أيٍّ «ميت» تأكّد أنه لا يُقرأ عبر اسمٍ غير مباشر أو سلسلة
> نصّيّة أو ملفّ `.set` — الشكّ يعني «حيّ». (٣) احصر كلّ تقرير CSV
> يكتبه الـ EA، صنّفه جوهريّ مقابل تشخيصيّ-منتهي-الغرض، وافحص
> تحديدًا `BrokerContractRealityAudit` و`BrokerRebasedPaperComparison`
> و`BrokerTradeManagementEventTimeline` و`S00_FvgDetection_Diagnostics`
> — هل لها قارئ/غرض باقٍ؟ (٤) أبرِز المخاطر: مدخلٌ ميتٌ يظهر في
> `.set` محفوظ، تقريرٌ منتهٍ يقرؤه فحصُ نزاهة، إلخ. أخرِج تقريرًا
> فيه جدول مدخلات وجدول تقارير باقتباسات `المسار:السطر`، وملخّصُ
> أعداد، وقائمةُ مخاطر — **دون اقتراح كود ودون حذف أيّ شيء**.
