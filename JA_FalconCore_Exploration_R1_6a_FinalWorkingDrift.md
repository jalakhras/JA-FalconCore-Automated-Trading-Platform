# JA FalconCore — استكشاف: لماذا انحرف `FinalWorkingNetUSD` في R1.6a؟
## مهمّة استكشاف (قراءة فقط)

> **مهمّة استكشاف فقط — لا تعديل، لا حذف، لا commit، لا تراجع
> (`git revert`/`checkout`)، لا بناء. تُشغَّل في Plan Mode.**
> تحليل كود/diff بحت. أخرِج التقرير في ردّك.
> **الحالة:** تغييرات R1.6a موجودة في working copy، **غير
> مُنفَّذة كـ commit**. تُترَك كما هي — هذا الاستكشاف يقرؤها فقط.

---

## السياق

مواصفة R1.6a حذفت نداءات كتابة التقارير التشخيصيّة المنتهية —
المفترَض «صفر تغيير سلوك». لكنّ اختبار التكافؤ كشف انحرافًا:

- `BrokerActualNetUSD` = 321.92 — **ثابت** (سلوك البروكر سليم).
- `RawNetUSD` = 1233.88 — **ثابت**.
- `TotalTrades` = 1146 — **ثابت**.
- `FinalWorkingNetUSD` = **42.60 → 1612.07** — انحرافٌ كبير،
  والقيمة الجديدة أعلى من `RawNetUSD` نفسه.

`FinalWorkingNetUSD` يأتي من `record.falcon_emergency_after_net_usd`
— الحقل الذي تُصفّره طبقة الـ emergency (Apply #11) للصفقات
المعلَّمة BLOCKED. قفزتُه من 42.60 إلى 1612.07 تعني أن **التصفير
لم يعد يحدث** — كأن الـ emergency لم يعد يلتصق.

`git diff --stat` يُظهر أن R1.6a لمس 7 ملفّات. **`Risk/FalconRiskLifecycleProcessor.mqh`
ليس بينها** (طبقة الـ emergency الأساسيّة لم تُمسّ — جيّد). لكنّ
**`Execution/FalconBrokerEntryBridge.mqh` تغيّر 41 سطرًا** — وهذا
غير متوقَّع لمهمّة «حذف تقارير»، وهو الخيط الأرجح.

## الهدف

تحديد — من الكود والـ diff، لا التخمين — **ما الذي حُذف في R1.6a
وتسبّب في توقّف تصفير الـ emergency**، ومن أين بالضبط (الـ Bridge؟
الـ ReportWriter؟ الـ `.mq5`؟). الجواب يحدّد العلاج: إصلاحٌ صغير
يستعيد التكافؤ، أم تراجعٌ جزئيّ عن حذفٍ بعينه.

## محاور الاستكشاف

### المحور ١ — تفصيل تغييرات الـ Bridge (41 سطرًا)
نفّذ `git diff Execution/FalconBrokerEntryBridge.mqh` واقرأ كلّ
سطرٍ محذوف. لكلّ كتلة محذوفة: هل هي **نداءُ كتابة تقرير بحت**، أم
تحوي **عملًا غير تقريريّ** — إسناد حقل، تحديث عدّاد، تعديل حالة؟
اقتبس الكتل غير التقريريّة المحذوفة مع `المسار:السطر`.

### المحور ٢ — تتبّع `falcon_emergency_after_net_usd` والتصاق الـ emergency
- أين يُسنَّد `falcon_emergency_after_net_usd`؟ وأين يُقرأ
  العَلَم الذي يُفعّل الـ emergency (`m_tle_emergency_active` أو
  ما يماثله)؟
- هل أيٌّ من الكتل المحذوفة في R1.6a (في أيّ ملفّ) كان **يكتب**
  هذا الحقل، أو **يضبط/يقرأ** عَلَم الالتصاق، أو يستدعي دالّةً
  تفعل ذلك؟
- بالأخصّ: دوالّ `Append` المحذوفة للتقارير المنتهية
  (`EmergencyTriggers`، `TierTransitions`، `PaperStateSnapshot`)
  — هل كان جسمُ أيٍّ منها يفعل عملًا محاسبيًّا/حالةً إلى جانب
  كتابة الصفّ؟ أم كتابةً بحتة؟

### المحور ٣ — مَن يغذّي الـ emergency الآن مقابل قبل R1.6a
قارن: قبل R1.6a، ما المسار الذي كان يُفعّل التصاق الـ emergency
ويُصفّر الصفقات؟ وهل R1.6a حذف حلقةً من ذلك المسار؟ حدّد الكتلة
المحذوفة المسؤولة بدقّة `المسار:السطر`.

### المحور ٤ — الحكم
- ما الكتلة (أو الكتل) المحذوفة التي أوقفت تصفير الـ emergency؟
- هل كانت تلك الكتلة **منطقًا حقيقيًّا متخفّيًا داخل كود تقرير**
  (لا يجب أن يُحذف)، أم نداءَ تقريرٍ بريئًا وانحراف
  `FinalWorkingNetUSD` سببُه شيءٌ آخر؟
- العلاج المقترَح مفهوميًّا (دون كتابة كود): استعادة الكتلة
  غير التقريريّة المحذوفة فقط، مع إبقاء حذف الكتابة التقريريّة؟
  أم غير ذلك؟

## صيغة الإخراج

تقرير منظّم — محور بمحور — باقتباسات `git diff` / كود قصيرة مع
`المسار:السطر`. في النهاية:
- **الكتلة/الكتل المسؤولة** عن انحراف `FinalWorkingNetUSD`،
  محدّدةً بدقّة.
- **الحكم:** منطقٌ حقيقيّ متخفٍّ حُذف خطأً، أم سببٌ آخر.
- **اتّجاه العلاج** — دون كتابة كود.
- أيّ غموض.

**لا تكتب كودًا، لا تعدّل، لا تتراجع، لا تنفّذ commit** — تشخيصٌ
فقط. على ضوئه تُكتب مواصفة الإصلاح.

---

## ملحق — Prompt جاهز لـ Claude Code

> فعّل وضع Plan Mode أوّلًا — مهمّة استكشاف قراءة-فقط، لا تعدّل ولا
> تحذف ولا تتراجع ولا تنفّذ commit ولا تبنِ. تغييرات R1.6a في
> working copy غير مُنفَّذة — اتركها كما هي، اقرأها فقط. اقرأ
> `JA_FalconCore_Exploration_R1_6a_FinalWorkingDrift.md` ونفّذ
> الاستكشاف. المشكلة: تنظيف تقارير R1.6a كان يجب أن يكون «صفر
> تغيير سلوك»، لكن `FinalWorkingNetUSD` انحرف 42.60 → 1612.07
> (بينما `BrokerActualNetUSD` و`RawNetUSD` و`TotalTrades` ثابتة).
> `FinalWorkingNetUSD` يأتي من `falcon_emergency_after_net_usd`
> الذي تُصفّره طبقة الـ emergency — قفزتُه تعني توقّف التصفير.
> `git diff --stat` يُظهر أن `Execution/FalconBrokerEntryBridge.mqh`
> تغيّر 41 سطرًا رغم أن المهمّة «حذف تقارير». نفّذ
> `git diff Execution/FalconBrokerEntryBridge.mqh` (وبقيّة
> الملفّات السبعة) واقرأ كلّ سطرٍ محذوف: ميّز نداءَ كتابة التقرير
> البريء عن أيّ عملٍ غير تقريريّ محذوف (إسناد حقل، تحديث عَلَم
> حالة، تعديل عدّاد محاسبيّ). تتبّع `falcon_emergency_after_net_usd`
> وعَلَم التصاق الـ emergency: هل حذفت R1.6a كتلةً كانت تُفعّل
> الالتصاق أو التصفير؟ افحص بالأخصّ أجساد دوالّ `Append` المحذوفة
> (`EmergencyTriggers`/`TierTransitions`/`PaperStateSnapshot`) —
> هل كانت تفعل عملًا محاسبيًّا إلى جانب الكتابة؟ أخرِج تقريرًا
> يحدّد الكتلة المسؤولة بدقّة `المسار:السطر`، والحكم (منطقٌ متخفٍّ
> حُذف خطأً أم سببٌ آخر)، واتّجاه العلاج — **دون كتابة كود ولا
> تعديل ولا commit**.
