# مواصفة إغلاق R1.6a

> **النوع:** إغلاق مرحلة — commit واحد + تحديث وثائق.
> **الإصدار:** R1.6a (هذا commit الإغلاق — لا رفع إصدار).
> **يسبقها:** `Exploration_R1_6a_FinalWorkingDrift.md`،
> `Spec_R1_6a_EquivalenceRetest.md`، وتشغيلَا أبريل (Prot ON و Prot OFF).

---

## ١. الهدف

إغلاق R1.6a بـ commit واحد على فرع `refactor`، مع توثيقٍ كاملٍ في الـ
Review Notes والذاكرة والـ Backlog لما تعلّمناه: تعريف اختبار التكافؤ
الصحيح، عيب المفتاحين، وتصنيف رقم 42.60.

---

## ٢. السياق الموجز

| الدليل | الخلاصة |
|---|---|
| الاستكشاف (line-by-line) | R1.6a لمست صفر أسطر في كود الحماية/Risk/المنسّق |
| إعادة الاختبار: Raw/Broker/Trades | متطابقة مع الـ baseline (1233.88 / ~321.9 / 1146) |
| إعادة الاختبار: قياس مباشر للمفتاحين | المفتاحان لا يطفئان الطبقة الورقيّة (115 و214 صفقة عُدِّلت رغم OFF) |
| المعرفة #١ | `FinalWorkingNetUSD` رقم ورقيّ مسموم — لا يصلح بوّابة قبول |

الاستنتاج: R1.6a **محايد سلوكيًّا**. الانحراف 42.60→1612 ليس من R1.6a؛
هو من إضافة محرّك الحماية في R1.5a (تغيّر تعريف `FinalWorking` ضمنيًّا
ليحمل تعديلات الحماية/الـ runner) + عيب gate في المفتاحين. اختبار التكافؤ
الأصليّ كان معيبًا بنيويًّا (قارن قيمةً عبر حدودِ تعريفِ المقياس).

---

## ٣. القيود الصارمة

- **لا تغيير كود** في هذا الـ commit. تنظيف R1.6a الموجود في الـ working
  copy كما هو — لا إضافة، لا إصلاح للعيب الجديد، لا أيّ شيء آخر.
- **لا إعادة كتابة تاريخ git.** commit واحد جديد فقط.
- **لا رفع `EA_VERSION_TAG`** — يبقى `R1_6a`.
- **لا تنظيف إضافيّ ولا تحسينات** — أيّ فكرة → Backlog.
- تسلسل سلسلة `Apply*` لا يُمسّ.

---

## ٤. التحديثات المطلوبة قبل الـ commit

### أ. الـ Review Notes — `Docs/ReviewNotes/R1_6a_Review_Notes.md`

- بند اختبار التكافؤ: من `PENDING` إلى **`PASS`** مع التوضيح:
  - **المعايير الصحيحة المُعتمَدة:** `RawNetUSD` + `BrokerActualNetUSD`
    + `TotalTrades` + تكافؤ صفوف الـ Reconciliation
    (`VisibleRawNetUSDDiff` ≈ 0، `VisibleFinalNetUSDDiff` ≈ 0). الأربعة
    تطابقت بين الـ baseline وR1.6a.
  - **`FinalWorkingNetUSD` رقم مراقَب، لا بوّابة** — اعتماده كبوّابة في
    الاختبار الأوّل كان خطأً منهجيًّا (معرفة #١: الورقيّ مسموم).
  - **تفسير الانحراف 42.60→1612.07:** ليس انحدارَ R1.6a؛ سببه (أ) تغيّر
    تعريف `FinalWorking` ضمنيًّا في R1.5a حين أُضيف محرّك الحماية
    (صار الرقم يحمل تعديلات الحماية/الـ runner)، (ب) المفتاحان
    `Enable_TradeManagement`/`Enable_S01_Protection` لا يطفئان الطبقة
    الورقيّة فعلًا — تشتغل بصرف النظر.

### ب. قائمة Problems — أضف عيبًا جديدًا

> **#6 — عائلة kill-switch (نمط #4):** `Enable_TradeManagement=false`
> و`Enable_S01_Protection=false` **لا يوقفان** طبقة الحماية/الـ runner
> الورقيّة. الأدلّة:
> - تشغيل أبريل Prot ON: ProtectionActivated=662، Runner=196، FinalWorking=1612.07.
> - تشغيل أبريل Prot OFF: ProtectionActivated=662، Runner=188، FinalWorking=1609.36.
> - 115 صفقة عُدِّل صافيها بالحماية و214 بالـ runner، رغم المفتاحين OFF.
> - البروكر مستقرّ بين التشغيلين — العيب في الـ gate الورقيّ، لا في
>   مسار التنفيذ.
>
> **حاجب لاختبارات العزل القادمة:** يجب إصلاح الـ gate قبل أيّ اختبار
> يُقاس فيه أثر محرّكٍ مُفرد.

### ج. `Ideas_Backlog`

- إلزام حفظ `.set` + `ReportTester.xlsx` لكلّ تشغيل (يسدّ مخاطرة R3).
- وسم اسم التقرير بقيم المدخلات الفارقة (`Enable_TM`، `Enable_Protection`،
  ...) — يمنع خلط تشغيلين بإعدادين مختلفين تحت اسمٍ واحد.
- إعادة تصنيف 42.60: رقمٌ تاريخيّ من حقبة قبل R1.5a (قبل وجود كود
  محرّك الحماية) — **لا يصلح مرجعًا لـ R1.5a+**. عند الحاجة لمرجعٍ
  «بلا حماية» على بناءٍ حديث، يتطلّب الأمر إصلاح الـ gate أوّلًا.

### د. ملفّ ذاكرة المشروع — إصدار جديد `R1_6a_closed`

- §٢: أضف فقرة قصيرة عن إغلاق R1.6a (الاستكشاف، إعادة الاختبار، عائلة
  العيوب المكتشَفة).
- §٣ (الحالة): «آخر commit مستقرّ: **R1.6a**». «الفرع: refactor». «الخطوة
  التالية: بناء الاستراتيجيّة الحقيقيّة».
- §٤ (خريطة البناء): R1.6a ⇒ **✓ مُغلقة**.
- §٥ (المعارف): وسّع المعرفة #١ بسطر: «الـ kill-switches قد تكون
  decorative — يجب التحقّق من فاعليّة الـ gate قبل الاعتماد عليه».
- §٧ (Problems): أضف #6 الجديد، أكِّد بقاء #2/#3/#4/#5.
- §٩: «الأولويّة: بناء الاستراتيجيّة الحقيقيّة».

---

## ٥. الـ commit

- الفرع: `refactor`.
- المحتوى: تنظيف R1.6a الموجود في الـ working copy + تحديثات §٤.
- النصّ المقترح:

  ```
  R1.6a — تنظيف نداءات التقارير التشخيصيّة المنتهية

  - حذف نداءات كتابة ~24 تقريرًا تشخيصيًّا منتهيًا.
  - تكافؤ سلوكيّ مُثبَت: Raw/Broker/Trades مطابقة للـ baseline،
    وتكافؤ صفوف الـ Reconciliation ≈ 0.
  - الاستكشاف أثبت حياد الكود سطرًا سطرًا (0 أسطر في كود
    الحماية/Risk/المنسّق).
  - عيب جديد مُسجَّل (#6، نمط #4): Enable_TradeManagement
    و Enable_S01_Protection لا يطفئان الطبقة الورقيّة —
    حاجب لاختبارات العزل القادمة.
  - رقم 42.60 أُعيد تصنيفه: حقبة ما قبل R1.5a، لا يصلح
    مرجعًا لبنى R1.5a+.
  ```

---

## ٦. معايير القبول

- `git log -1 --oneline` على `refactor` يُظهر سطر R1.6a.
- `Docs/ReviewNotes/R1_6a_Review_Notes.md`: PENDING ⇒ PASS مع التوضيح.
- Problems: العيب #6 مُسجَّل.
- `Ideas_Backlog`: البنود الثلاثة مُضافة.
- ملفّ الذاكرة: إصدار جديد يعكس إغلاق R1.6a والخطوة التالية.
- لا تغيير في أيّ ملفّ `.mq5` أو `.mqh` غير ما هو موجود سلفًا في الـ
  working copy من تنظيف R1.6a.

---

## ٧. بروتوكول الإغلاق (قاعدة #٨)

- **مراجعة Problems:** بنود #2/#3/#4/#5 سابقة لـ R1.6a وغير حاجبة لهذا
  الـ commit. تبقى مفتوحة. أُضيف #6 الجديد.
- **تسجيل الأفكار:** §٤-ج أعلاه.
- **ملفّ المراجعة:** `R1_6a_Review_Notes.md` (نفسه، مُحدَّث).

---

## ٨. ملحق — Prompt لـ Claude Code

```
السياق: نحن على فرع `refactor`. الـ working copy يحوي تنظيف R1.6a
(حذف نداءات كتابة تقارير منتهية). الكود لم يُنفَّذ كـ commit بعد.
نريد إغلاق R1.6a الآن بـ commit واحد + تحديثات وثائق.

افعل بالضبط ما يلي، ولا شيءَ آخر:

١. لا تلمس أيّ ملفّ .mq5/.mqh. تنظيف R1.6a يبقى كما هو في الـ working copy.

٢. حدّث Docs/ReviewNotes/R1_6a_Review_Notes.md:
   - بند اختبار التكافؤ: PENDING → PASS.
   - أضف فقرة "المعايير المُعتمَدة": Raw + Broker + Trades + reconciliation diffs ≈ 0
     (مع القيم: 1233.88 / ~321.9 / 1146 / ≈0).
   - أضف فقرة "تفسير الانحراف 42.60→1612.07": تغيّر تعريف ضمنيّ في R1.5a
     (إضافة الحماية) + عيب gate في المفتاحين.

٣. أضف العيب #6 إلى قائمة Problems (في ملفّ الذاكرة الحاليّ §٧ أو حيث تُسجَّل):
   "#6 — Enable_TradeManagement=false و Enable_S01_Protection=false
    لا يطفئان طبقة الحماية/الـ runner الورقيّة (نمط #4). حاجب لاختبارات
    عزل المحرّكات القادمة. الأدلّة: تشغيلَا أبريل Prot ON/OFF أعطيا
    1612.07 و1609.36 مع ProtectionActivated=662 في الحالتين."

٤. أضف إلى Ideas_Backlog (ملفّ المشروع المخصّص أو §٨ من ملفّ الذاكرة):
   - "إلزام حفظ .set + ReportTester.xlsx لكلّ تشغيل."
   - "وسم اسم التقرير بقيم المدخلات الفارقة (Enable_TM، Enable_Protection)."
   - "إعادة تصنيف 42.60: حقبة قبل R1.5a، لا يصلح مرجعًا لـ R1.5a+."

٥. أنشئ نسخة جديدة من ملفّ ذاكرة المشروع باسم يعكس إغلاق R1.6a
   (مثلًا JA_FalconCore_Project_Memory_R1_6a_closed.md):
   - §٣: آخر commit مستقرّ = R1.6a، الخطوة التالية = الاستراتيجيّة الحقيقيّة.
   - §٤: R1.6a ⇒ ✓ مُغلقة.
   - §٥: وسّع #١ بسطر عن decorative kill-switches.
   - §٧: أضف #6 الجديد.
   - §٩: الأولويّة = بناء الاستراتيجيّة الحقيقيّة.

٦. نفّذ commit واحدًا على `refactor` بنصّ:
   (انسخ النصّ من §٥ من المواصفة كما هو)

٧. توقّف. أظهِر: git status، git log -1 --oneline، git diff HEAD~1 --stat.

ممنوع: أيّ تغيير لكود، أيّ commit ثانٍ، أيّ تنظيف إضافيّ، أيّ "تحسينات
صغيرة"، أيّ git rebase أو reset. ما لم تُذكر هنا — لا تفعله.
```
