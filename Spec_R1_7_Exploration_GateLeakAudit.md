# مواصفة استكشاف R1.7 — تسرّب الـ Gate في الطبقة الورقيّة

> **النوع:** استكشاف قراءة-فقط (Plan Mode). لا كود، لا تعديل، لا commit،
> لا بناء. ينتج تقريرًا واحدًا فقط.
> **الإصدار:** قبل R1.7 — لا رفع `EA_VERSION_TAG`.
> **يسبقه:** إغلاق R1.6a (commit `24b19ed`).

---

## ١. الهدف

تحديدٌ دقيق — بـ `file:line` — لكلّ موضعٍ تشتغل فيه الطبقة الورقيّة
دون أن تحترم المفتاح المُعلَن لإطفائها. الهدف ليس إصلاح أيّ شيء؛ بل
**رسم خريطة المواضع**، ليُحدَّد حجم إصلاح R1.7 بدقّة.

السؤال المركزيّ: **عند `Enable_TradeManagement=false` و
`Enable_S01_Protection=false` و`EnableStrategy_FvgMicroRetest=false`،
ما الذي يجب أن يخمد، ولماذا لم يخمد؟**

---

## ٢. السياق — الدليل الرقميّ المرصود

من تشغيلَي أبريل (نفس الكود، إعداد متغيّر):

| المؤشّر | Prot ON | Prot OFF | الفرق |
|---|---|---|---|
| `ProtectionActivatedTrades` | 662 | 662 | 0 |
| `RunnerActivatedTrades` | 196 | 188 | 8 |
| صفقات `ProtectionNetUSD ≠ RawTradeUSD` | (مرتفع) | 115 | — |
| صفقات `RunnerNetUSD ≠ RawTradeUSD` | (مرتفع) | 214 | — |
| `FinalWorkingNetUSD` | 1612.07 | 1609.36 | 2.71 |
| `BrokerActualNetUSD` | 321.92 | 321.68 | 0.24 |

طبقة البروكر مستقرّة (الـ gate يعمل هناك أو المسار خاملٌ في الـ harness).
الطبقة الورقيّة تشتغل بصرف النظر — هنا التسرّب.

---

## ٣. القيود الصارمة

- **لا تعديل**: لا حذف، لا إضافة، لا إعادة تنسيق، لا commit، لا بناء (F7).
- **لا اقتراح إصلاحٍ في الكود** — التقرير يصف فقط: «هنا الموضع، هذا حاله».
- **لا تخمين** عن النيّة الأصليّة للمصمّم — تقرير حالةٍ موضوعيّ، مع تمييزٍ
  واضح بين «المرصود» و«المُحتمَل».
- **لا توسيع** للنطاق إلى Risk/emergency أو سلسلة `Apply*` أو المنسّق
  ككلّ — التركيز على نقاط التسرّب الورقيّة فقط.

---

## ٤. المحاور — الأسئلة التي يجب أن يجيبها التقرير

### المحور الأوّل — عدّاد `ProtectionActivatedTrades`

١. أين بالضبط (`file:line`) يُزاد العدّاد؟ (`m_totals.protection_activated_trades++`
أو ما يماثله.)
٢. ما الشرط الذي يسبق الزيادة؟ (`if (...)`)
٣. هل هذا الشرط يقرأ `Enable_S01_Protection`، أو `Enable_TradeManagement`،
أو أيّ مفتاحٍ آخر؟
٤. إن لا — ما الذي يسبق هذه النقطة في سلسلة الاستدعاء، وهل أيّ
nullable يحرس الدخول لها أصلًا؟

### المحور الثاني — عدّاد `RunnerActivatedTrades`

نفس الأسئلة الأربعة، بنفس الترتيب.

### المحور الثالث — حقل `ProtectionNetUSD` في `TradeLifecycle`

١. أين بالضبط يُسنَّد `record.protection_net_usd` (أو الاسم الفعليّ)؟
٢. ما الذي يحسبه — مجرّد نسخٍ من `raw_trade_usd`، أم نتيجة منطق حماية فعليّ؟
٣. ما الذي يسبق الإسناد؟ هل هناك حارسٌ على مفتاح؟
٤. الـ 115 صفقة التي عُدِّل صافيها (Prot OFF): ما السمة المشتركة بينها؟
(`EmergencyStatus`؟ تجاوز عتبة `Protection_TriggerRMultiple`؟ شيء آخر؟)

### المحور الرابع — حقل `RunnerNetUSD`

نفس أسئلة المحور الثالث. الـ 214 صفقة المُعدَّلة (Prot OFF) — ما سمتها؟

### المحور الخامس — قراءة المفاتيح الثلاثة في الكود

لكلّ مفتاح: `Enable_TradeManagement`، `Enable_S01_Protection`،
`EnableStrategy_FvgMicroRetest`:

١. كم موضعًا في الكود يقرأ هذا المفتاح؟
٢. ما المسارات التي يحرسها فعلًا (إن وُجدت)؟
٣. ما المسارات التي **يدّعي حراستها** (بحسب اسم المفتاح) لكنّه لا
يحرسها فعلًا؟

### المحور السادس — محاكاة Legacy الظلّيّة (#4)

١. أين تشتغل محاكاة `FvgMicroRetest` الظلّيّة؟
٢. هل تُستدعى من دالّة `OnTick` أو من المنسّق أو موضعٍ ثالث؟
٣. ما يحرسها (إن وُجد)؟

---

## ٥. ما يجب أن يحتويه التقرير

ملفّ واحد، اسمه `Exploration_R1_7_GateLeakAudit.md`، يحوي:

- **رأس:** الإصدار، التاريخ، الفرع، الـ commit (`24b19ed`).
- **محورٌ لكلّ من السادسة أعلاه**، ترتيبًا — مع `file:line` صريحة لكلّ
  ادّعاء، ومقتطف الكود المعنيّ (لا أكثر من ٥ أسطر للمقتطف الواحد).
- **جدول الخلاصة:** صفّ لكلّ من العدّادين والحقلين والمفاتيح الثلاثة
  ومحاكاة legacy — يقول لكلٍّ: «أين»، «يحرسه الآن: ___»، «يجب أن
  يحرسه: ___»، «الحارس المفقود: ___».
- **تقدير حجم الإصلاح:** عدد المواضع التي تحتاج حارسًا، تقديرٌ بعدد
  الأسطر المُضافة، هل الإصلاح متمركزٌ في ملفّ/ملفّين أم منتشر.
- **الغموض:** ما لم يُحسم بقراءة الكود — ولماذا.

---

## ٦. معايير قبول الاستكشاف

- التقرير يجيب الأسئلة الست (محاورها) بـ `file:line` صريحة، لا تعميمات.
- مقتطفات الكود قصيرة (≤ ٥ أسطر) ودقيقة (تَنسخ من الملفّ).
- جدول الخلاصة يحوي ٦ صفوف على الأقلّ (عدّادان + حقلان + ٣ مفاتيح +
  محاكاة legacy — ٧ صفوف، أو ٦ إن دُمج عدّاد ومحاكاة).
- تقدير حجم الإصلاح موجود حتى لو كان «غير قابل للتقدير لـ X»، يُذكر السبب.
- لا اقتراح إصلاحٍ في الكود — فقط وصف الحالة.

---

## ٧. بروتوكول الإغلاق

- مراجعة Problems: تأكَّد أن العيبين #4 و#6 ما زالا مفتوحين — هذا
  الاستكشاف لا يُغلقهما، يُمهّد للإصلاح.
- تسجيل أيّ ملاحظةٍ جانبيّة طارئة → Ideas Backlog، لا تنفيذ فيها.
- يُحفظ التقرير في `Docs/Explorations/` (أو حيث تُحفظ الاستكشافات).

---

## ٨. ملحق — Prompt لـ Claude Code

```
السياق: نحن على فرع `refactor`، آخر commit R1.6a (24b19ed). يجب أن
يكون هذا استكشاف Plan Mode قراءة-فقط، لا تنفيذ.

التعليمات بهذا الترتيب الحرفيّ:

١. اقرأ كاملًا الملفّ المرفق `Spec_R1_7_Exploration_GateLeakAudit.md`.
   هو المرجع الوحيد لهذا العمل — الهدف، المحاور الستّة، القيود،
   متطلّبات التقرير، معايير القبول. كلّ ما يلي هنا تعليماتٌ تشغيليّة،
   ليست بديلًا عن قراءته.

٢. التزم بـ:
   - §٣ القيود الصارمة (لا تعديل، لا commit، لا بناء، لا اقتراحات
     إصلاحٍ في الكود، لا توسيع للنطاق إلى Risk/emergency أو
     سلسلة Apply*).
   - §٤ المحاور الستّة كاملةً — لا تتجاوز محورًا.
   - §٥ بنية التقرير المطلوبة.
   - §٦ معايير القبول.

٣. ابدأ بـ grep في المستودع للسلاسل التالية لرسم خريطة المواضع
   المرشّحة قبل الدخول للقراءة العميقة:
     protection_activated_trades
     runner_activated_trades
     protection_net_usd
     runner_net_usd
     Enable_S01_Protection
     Enable_TradeManagement
     EnableStrategy_FvgMicroRetest

٤. أنتج تقريرًا واحدًا فقط — `Docs/Explorations/Exploration_R1_7_GateLeakAudit.md`
   — بالشكل المُحدَّد في §٥ من المواصفة.

٥. بعد كتابة التقرير: توقّف. أظهر مسار الملفّ. لا تنفّذ `git add`،
   ولا commit، ولا بناء.

عند أيّ تعارضٍ بين تعليماتي هنا والمواصفة: **المواصفة تحكم**.
عند أيّ غموضٍ في النطاق: ارفعه في قسم «الغموض» داخل التقرير، لا
تُوسّع النطاق من تلقاء نفسك.
```
