# FalconCore — Feature Inventory at v0.57.4

> Read-only snapshot generated by scanning `JA_FalconCore_Automated_Trading_Platform.mq5`.
> No source code was modified.
> Working tree: `dev` @ commit `5a7e961` (v0.57.4).

---

## 1. Version Tags

| Constant | Value | Source line |
|---|---|---|
| `EA_VERSION_TAG` | `"v0.57.4"` | line 11 |
| `EA_BUILD_TAG` | `"StructuralBreakeven"` | line 12 |

Confirmed: file is at **v0.57.4**.

File size: **17,343 lines** (single `.mq5`).

---

## 2. Input Variables (by input group)

Total inputs: **46** (across 6 groups).

### 2.1  Group `"01 - EA Safety & Risk / إعدادات المستخدم الأساسية"`  (line 1561)

| Name | Type | Default | Inline comment |
|---|---|---|---|
| `EnableRealExecution` | `bool` | `false` | HARD SAFETY: remains false during shadow/candidate stages. |
| `UseFixedLot` | `bool` | `true` | simple user mode: fixed lot first. |
| `FixedLotSize` | `double` | `0.01` | — |
| `UseAutoCapitalDetection` | `bool` | `true` | true = use account balance later; false = ManualCapital. |
| `ManualCapital` | `double` | `10000.0` | — |
| `UseDailyLossLimit` | `bool` | `true` | Daily safety ON/OFF: enables daily loss protection and risk reference. |
| `UseFixedDailyLossAmount` | `bool` | `false` | true = use FixedDailyLossAmount USD; false = use DailyLossPercentOfCapital. |
| `FixedDailyLossAmount` | `double` | `100.0` | Fixed daily loss limit in USD when UseFixedDailyLossAmount=true. |
| `DailyLossPercentOfCapital` | `double` | `3.0` | Daily loss limit as percent of capital when UseFixedDailyLossAmount=false. |

### 2.2  Group `"01a - Close Safety / حماية الإغلاق"`  (line 1575)

| Name | Type | Default | Inline comment |
|---|---|---|---|
| `StopNewTradesBeforeClose` | `bool` | `false` | Stop new trades near close. |
| `StopNewTradesBeforeCloseMinutes` | `int` | `5` | Minutes before close. |
| `CloseTradesBeforeDailyClose` | `bool` | `false` | Close trades before daily close. |
| `DailyCloseSafetyMinutes` | `int` | `5` | Minutes before daily close. |
| `CloseTradesBeforeWeekend` | `bool` | `false` | Close trades before weekend. |
| `WeekendCloseSafetyMinutes` | `int` | `5` | Minutes before weekend close. |

### 2.3  Group `"02 - Execution Stage / مرحلة التنفيذ"`  (line 1820)

| Name | Type | Default | Inline comment |
|---|---|---|---|
| `EnableShadowMode` | `bool` | `true` | Observe only. No real orders. |
| `EnablePaperMode` | `bool` | `false` | Internal simulated trades later. |
| `MaxTradesPerDay` | `int` | `3` | — |
| `MaxOpenPositions` | `int` | `1` | — |

> Note (line 1814): `FalconMagicNumber` input was intentionally removed in v0.56.0; engine-specific `FC_MAGIC_*` constants are the only valid broker identity source.

### 2.4  Group `"02.1 - Enable Virtual Trailing Bridge"`  (line 1836)

| Name | Type | Default | Inline comment |
|---|---|---|---|
| `EnableVirtualTrailingBridge` | `bool` | `true` | v0.57.2 trailing exit |
| `VirtualTrailingStartPoints` | `double` | `8.0` | v0.57.3 recalibrated (was 30) — profit points before trailing arms |
| `VirtualTrailingDistancePoints` | `double` | `6.0` | v0.57.3 recalibrated (was 20) — trailing distance behind peak |
| `VirtualBreakevenAtPoints` | `double` | `4.0` | v0.57.3 recalibrated (was 15) — profit points to lock breakeven |
| `EnableStructuralBreakeven` | `bool` | `true` | v0.57.4 structural BE |
| `VirtualSwingLookbackBars` | `int` | `2` | half-window N (5-bar swing when N=2) |
| `VirtualSwingMaxLookback` | `int` | `40` | max closed bars to scan |

### 2.5  Group `"03 - Strategy Switches / تفعيل وإيقاف الاستراتيجيات"`  (line 1852)

| Name | Type | Default | Inline comment |
|---|---|---|---|
| `EnableStrategy_FvgMicroRetest` | `bool` | `true` | ShadowSmoke core winner candidate. Still no real execution. |
| `EnableStrategy_TailSmartReturn` | `bool` | `false` | استراتيجية ذيل العودة الذكي. |
| `EnableStrategy_MomentumCross820` | `bool` | `false` | استراتيجية تقاطع الزخم 8/20. |
| `EnableStrategy_CheckMarkLiquiditySweep` | `bool` | `false` | استراتيجية علامة الصح بعد سحب السيولة. |
| `EnableStrategy_DoubleBoxWickConfirmation` | `bool` | `false` | استراتيجية الصندوقين والشمعة الحاسمة. |
| `EnableStrategy_TouchTurnOpeningRange` | `bool` | `false` | استراتيجية لمس نطاق الافتتاح والارتداد. |
| `EnableStrategy_MagicLiquidityPitchfork` | `bool` | `false` | استراتيجية خطوط السيولة الذكية وشمعة الشوكة. |
| `EnableStrategy_DailyLiquidityBox` | `bool` | `false` | استراتيجية صندوق السيولة اليومي وشمعة الشوكة. |
| `EnableStrategy_QuickFlipOpeningLiquidity` | `bool` | `false` | استراتيجية الانعكاس السريع بعد شمعة السيولة. |
| `EnableStrategy_FSEPatternFailureEntry` | `bool` | `false` | استراتيجية الدخول المبكر بعد فشل النموذج. |
| `EnableStrategy_TwoLiquidityLines15M` | `bool` | `false` | استراتيجية خطّي السيولة والارتداد المؤكد. |
| `EnableStrategy_GoldenLiquidity5MEntry` | `bool` | `false` | استراتيجية مناطق السيولة الذهبية ودخول الخمس دقائق. |

### 2.6  Group `"04 - Reporting / التقارير"`  (line 1871)

| Name | Type | Default | Inline comment |
|---|---|---|---|
| `EnableMainReport` | `bool` | `true` | — |
| `ReportProfile` | `ENUM_FALCON_REPORT_PROFILE` | `FALCON_REPORT_LOCKPARITY` | — |
| `ReportModeTag` | `string` | `"EmergencyServerStopEnvelopeLockParityProbe"` | v0.56.9b safety restoration tag; v0.57.3 normalizes this to `"VirtualTrailingBridge"` in `FalconEffectiveReportModeTag()`. |
| `UseCommonFilesFolderForReports` | `bool` | `true` | — |
| `EnableVerboseExpertsLog` | `bool` | `true` | — |
| `EnableFastRuntimeSmokeMode` | `bool` | `true` | — |
| `RuntimeDiagnosticsEveryNTicks` | `int` | `1000` | — |

---

## 3. Classes (one-line purpose + size)

Total: **18 top-level classes**.

| # | Class | Lines | Purpose |
|---|---|---|---|
| 1 | `CFalconLogger` | 22 | Static logger wrapper around `Print` / `PrintFormat` with severity. |
| 2 | `CFalconMarketContext` | 200 | Live market state: symbol context, MTF data, tick refresh. |
| 3 | `CFalconCandleCache` | 123 | Bounded cache of closed M1–MN1 candles for indicator-free lookups. |
| 4 | `CFalconEvidenceFramework` | 232 | Evidence collection / packing for strategy decisions. |
| 5 | `CFalconRiskFoundation` | 72 | Input validation gate; blocks `EnableRealExecution` until guards pass. |
| 6 | `CFalconStrategyRegistry` | 221 | Registry of strategy entries with group + health classification. |
| 7 | `CFalconShadowExecutor` | 248 | Internal Shadow trade lifecycle executor (no broker orders). |
| 8 | `CFalconRuntimeSafetyGuard` | 184 | Pre-execution gate + no-lookahead validation. |
| 9 | `CFalconFirstShadowStrategyAdapterShell` | 126 | Adapter shell for the first strategy; bridges to `CFalconShadowExecutor`. |
| 10 | `CFalconFvgMicroShadowDetectorStub` | 289 | M5 FVG detector ("Stub" by name but fully functional). |
| 11 | `CFalconFvgMicroShadowCandidateBuilder` | 231 | Builds FVG candidates with quality scoring. |
| 12 | `CFalconFvgMicroRetestWatcher` | 427 | Watches FVGs for retest touches with age + freshness tracking. |
| 13 | `CFalconFvgMicroTradePlanStagingDryRun` | 289 | Dry-run staging of TradePlans (no OrderSend). |
| 14 | `CFalconFvgMicroShadowLifecycleSimulation` | 362 | Simulates the Shadow lifecycle TP1/SL/timeout outcomes. |
| 15 | `CFalconReportWriter` | **6,292** | Dominant class. Owns every report file, the per-trade `Apply*` layer chain, Summary, LockParity Decomposer, and all rollups. |
| 16 | `CFalconExecutionGuard` | 29 | Minimal pre-execution scaffold (no runtime enforcement yet). |
| 17 | `CFalconRiskTradeManagementArchitecture` | 70 | Architecture scaffold for risk-driven trade management. |
| 18 | `CFalconBrokerEntryBridge` | **2,735** | Tester-only broker layer: `OrderSend` entry + atomic emergency server SL/TP, OnTradeTransaction observation, managed close via `TryManagedCloseFromLifecycleRecord` / `ExecuteReverseClosePositionRequest`, and the v0.57.2/3/4 Virtual Trailing Exit Bridge with structural BE (`FindLastSwingLow` / `FindLastSwingHigh`). |

---

## 4. Major Engines / Features (status report)

### StructuralStop / TPBuilder / EarlyFailureExit  →  **VALIDATION-ONLY**
- `FalconStructuralStopValid(record)` exists (line 4291). Counters `structural_stop_valid_trades` / `structural_stop_invalid_trades` are tracked in Summary. No runtime SL change is applied — it's a per-trade validation lock from v0.28.1.
- `FALCON_TP_BUILDER_STATUS_V055` / `FalconTakeProfitSequenceValid` exist as validation-side audit only.
- **`EarlyFailureExit` is NOT present** in this build (no matches found for `EarlyFailureExit` or `early_failure_exit`).

### Three-Layer Emergency  →  **FULLY IMPLEMENTED (runtime-enforced)**
- `ApplyThreeLayerEmergencyApplication(record)` (line 13214). `FALCON_TLE_RUNTIME_ENFORCED = true`.
- Three layers: Layer 1 (consecutive losses), Layer 2 (daily R), Layer 3 (tier drawdown). Any-layer-triggers. Tier-aware, no fixed 5% hardcap.
- Emits `EmergencyTriggers.csv` on `TRIGGERED` records and a `falcon_emergency_triggered_layer` field on every trade record.

### Capital Tier System  →  **FULLY IMPLEMENTED**
- `ApplyCapitalTierFoundation(record)` (line 12766). `FalconCapitalTierName(balance)` maps balance to a tier (line 3935).
- 7 tier fields on the lifecycle record (`falcon_tier_at_entry`, `falcon_tier_min_balance`, `falcon_tier_max_losses`, `falcon_tier_max_daily_r`, `falcon_tier_max_drawdown_pct`, etc.) and tier transition events emitted to `TierTransitions.csv`.

### Runner / Protected Runner  →  **FULLY IMPLEMENTED (Paper side)**
- `ApplyPaperRuntimeRunnerApplication(record)` (line 12674). Status: `FALCON_RUNNER_MANAGER_STATUS_V055 = "ACTIVE_PAPER_RUNNER_MOON_MODE_LOCKED"`.
- MOON-mode proxy + TP2-proof gate. Per-trade fields: `paper_runner_state`, `paper_runner_activated`, `paper_runner_max_r`, `paper_runner_captured_points`, `paper_runner_additional_points`, `paper_runner_net_usd`.
- The v0.57.1 Runner Exit Price Probe in `CFalconReportWriter` classifies each runner trade into `RUNNER_EXIT_PRICE_WITHIN_MARKET_PATH` / `OUTSIDE_MARKET_PATH` / `PATH_DATA_INSUFFICIENT`.

### Session Boundary Guard  →  **MIXED**
- **Close-before-daily-close / before-weekend**: `ApplyDailyWeekendForceCloseUserOverridePaperEnforcement(record)` (line 10293) — fully implemented as Paper-side enforcement. Inputs in group 01a (`CloseTradesBeforeDailyClose`, `CloseTradesBeforeWeekend`).
- **No-new-entry-before-close**: `ApplyNoNewEntryUserOverridePaperEnforcement(record)` (line 10251) — fully implemented.
- **Dynamic recovery checkpoint**: foundation only. `FALCON_SESSION_BOUNDARY_CHECKPOINT_MODE = "FOUNDATION_ONLY_NO_RUNTIME_RESTORE"`. Measurement counters exist (`session_boundary_*` in `FalconReportTotals`) but no runtime restore yet.
- **Holiday handling: NOT PRESENT.** The string `holiday` appears only once in the entire file (line 1515) inside a comment about Strategy Tester UI date handling. No holiday calendar, no holiday-skip logic.

### Paper Protection  →  **FULLY IMPLEMENTED**
- `ApplyPaperRuntimeGuardApplication(record)` (line 12567) — guard layer.
- `ApplyPaperRuntimeSmartSLProtectionApplication(record)` (line 12597) — Smart SL / Protection layer.
- Fields: `paper_guard_*`, `paper_protection_*` (state, trigger, level, activated, net_usd). Status: `FALCON_PAPER_SM_POLICY = "VALIDATION_LOCK;PROTECTION_FIRST;TP2_IS_NOT_FINAL_TARGET"`.

### Broker Execution Bridge (v0.56.x)  →  **FULLY IMPLEMENTED (Tester-only)**
- `CFalconBrokerEntryBridge` class (2,735 lines, line 14608).
- Hard-gated by `EnableRealExecution && MQLInfoInteger(MQL_TESTER)`. Demo/Live blocked.
- Atomic entry with emergency server SL/TP envelope (no naked orders). Restored as the v0.56.9b baseline.
- `OnTradeTransaction` observation via `ObserveTradeTransactionExit`. Managed close via `TryManagedCloseFromLifecycleRecord` calling the factored helper `ExecuteReverseClosePositionRequest` (single OrderSend call site).
- Emits 5 broker reports: `BrokerExecutionLifecycle`, `BrokerTradeManagementEventTimeline`, `BrokerContractRealityAudit`, `BrokerRebasedPaperComparison`, `BrokerPaperTradeReconciliation`.

### Virtual Trailing Bridge (v0.57.2 / v0.57.3 / v0.57.4)  →  **FULLY IMPLEMENTED**
- `UpdateVirtualTrailingForOpenLinks(report_writer)` on the bridge. Called from OnTick after `FalconRunFvgMicroRuntimeShadowPipeline`, gated by `EnableVirtualTrailingBridge && EnableRealExecution && MQL_TESTER`.
- v0.57.2: trailing-stop ratchet behind peak + breakeven floor + close via same `ExecuteReverseClosePositionRequest` helper with comment prefix `JAFC572VT`.
- v0.57.3: thresholds recalibrated (8/6/4 vs 30/20/15) + spread-aware breakeven floor.
- v0.57.4: structural BE — `FindLastSwingLow` / `FindLastSwingHigh` over closed M5 bars (`shift ≥ 1`, no lookahead) anchor the BE floor on the most recent confirmed swing. `FIXED_FALLBACK` to v0.57.3 floor when no qualifying swing exists.
- Emits `VirtualTrailingExitLifecycle.csv` (one row per trailing close, with `BreakevenMode` = STRUCTURAL / FIXED_FALLBACK / "").

### Lock Parity Decomposer (v0.57.0 / v0.57.1)  →  **FULLY IMPLEMENTED (report-only)**
- v0.57.0: per-trade decomposition of `FinalWorking − Raw` into 7 layer deltas (Guard / Protection / Runner / LossCap / MarketClose / Emergency / Other), each classified `EXECUTABLE_*` / `NON_EXECUTABLE_ACCOUNTING` / pending-evidence. Emits `LockParityExecutabilityDecomposition.csv` + `LockParityExecutabilityRollup.csv`.
- v0.57.1: `LOCKPARITY` report profile (`FALCON_REPORT_LOCKPARITY = 3`); when active, only 4 CSVs emitted. Runner Exit Price Probe adds 5 diagnostic columns + Runner Executable / Non-Executable buckets.
- Hook: `UpdateLockParityTotals` + `AppendLockParityDecompositionRecord` called from `RegisterClosedTrade` AFTER every `Apply*` layer; `WriteLockParityExecutabilityRollup` called from `WriteFinalSummary`.

### Single Trade Loss Cap  →  **FULLY IMPLEMENTED (Calibrated, runtime-enforced)**
- `ApplyCalibratedSingleTradeLossCapEnforcement(record)` (line 12889).
- Per-trade fields: `falcon_single_trade_loss_cap_status`, `falcon_single_trade_loss_cap_blocked`, `falcon_single_trade_loss_cap_strict_cap_r`, `falcon_single_trade_loss_cap_calibrated_cap_r`, `falcon_single_trade_loss_cap_before_net_usd`, `falcon_single_trade_loss_cap_after_net_usd`.

### Additional engines worth noting
- **FVG Micro pipeline** (full chain): Detector → Candidate → RetestWatcher → TradePlanStaging → LifecycleSimulation. 5 dedicated classes, ~1,600 lines total. Driven by `EnableStrategy_FvgMicroRetest` (the only strategy enabled by default).
- **Dynamic Lot Sizing Model**: `ApplyDynamicLotSizingModel(record)` (line 13080), runtime-enforced (`falcon_dlm_runtime_enforced`).
- **Capital Flow Source Classification**: `ApplyCapitalFlowSourceClassification(record)` (line 13375).
- **FVG Quality Shadow Guard simulation**: `ApplyFvgQualityShadowGuardSimulation(record)` (line 12539).
- **Low-Capital Risk Feasibility Foundation**: `ApplyLowCapitalRiskFeasibilityFoundation(record)` (line 12793).
- **Weekly Stability**: summary-only counters via `FalconWeeklyStabilityWeekKey`.
- **Paper State Persistence**: foundation only (`FALCON_PERSISTENCE_STATUS = "PAPER_STATE_PERSISTENCE_FOUNDATION"`). Snapshot-only, no restore.

---

## 5. Report Types Emitted

The EA can emit up to **32 distinct report files** depending on the `ReportProfile`. Counts per profile (from `FalconReportNameCount`):

| Profile | File count |
|---|---|
| `MINIMAL` | 13 |
| `STANDARD` | 14 |
| `DEBUG` | 31 |
| `LOCKPARITY` | 4 (silenced via early-return guards in v0.57.1) |

Full list:

**Core (every profile):**
1. `TradeLifecycle.csv`
2. `Summary.csv`

**Tier / Emergency / Paper state (MINIMAL+):**
3. `TierTransitions.csv`
4. `EmergencyTriggers.csv`
5. `PaperStateSnapshot.csv`

**Broker (MINIMAL+):**
6. `BrokerExecutionLifecycle.csv`
7. `BrokerTradeManagementEventTimeline.csv`
8. `BrokerContractRealityAudit.csv`
9. `BrokerRebasedPaperComparison.csv`
10. `BrokerPaperTradeReconciliation.csv`

**Lock Parity (v0.57.0/1, every profile):**
11. `LockParityExecutabilityDecomposition.csv`
12. `LockParityExecutabilityRollup.csv`

**Virtual Trailing (v0.57.2/3/4, every profile; header gated on `EnableVirtualTrailingBridge && EnableRealExecution && MQL_TESTER`):**
13. `VirtualTrailingExitLifecycle.csv`

**Strategy Registry (STANDARD+):**
14. `StrategyRegistryDiagnostics.csv`

**Debug-only diagnostics (DEBUG profile only):**
15. `StartupBootstrap.csv`
16. `ReportCreationGuarantee.csv`
17. `MarketDiagnostics.csv`
18. `CandleCacheDiagnostics.csv`
19. `EvidenceDiagnostics.csv`
20. `ShadowDiagnostics.csv`
21. `NoLookaheadDiagnostics.csv`
22. `StrategyAdapterDiagnostics.csv`
23. `FvgMicroDetectorDiagnostics.csv`
24. `FvgMicroShadowCandidateDiagnostics.csv`
25. `FvgMicroRetestWatcherDiagnostics.csv`
26. `FvgMicroTradePlanStagingDiagnostics.csv`
27. `FvgMicroLifecycleSimulationDiagnostics.csv`
28. `ReportCalibrationCleanupGate.csv`
29. `RuntimeReportVerification.csv`
30. `FvgMicroRuntimeSmokeTest.csv`
31. `FvgMicroRuntimeReportAudit.csv`

**Auto-emitted period metadata (any non-LOCKPARITY profile):**
32. `AutoPeriodManifest.csv` (silenced in LOCKPARITY profile via v0.57.1 guard)

---

## 6. Object Counts

| Category | Count |
|---|---|
| `#define` directives | **693** |
| Top-level `struct` declarations | **22** |
| Top-level `class` declarations | **18** |
| Top-level `enum` declarations | **22** |
| Globals (`g_*` at file scope) | **85** |
| Input variables | **46** (across 6 input groups) |
| `Apply*` lifecycle layer functions | **12** (the per-trade layer chain in `CFalconReportWriter`) |

### Globals breakdown
- 8 report-period state globals (`g_report_period_*`, `g_report_initial_*`, `g_report_active_*`)
- 9 FVG candidate evaluation counters (`g_fvg_micro_*`)
- 9 FVG quality calibration counters (`g_fvg_quality_*`)
- 11 FVG quality distribution stats (size, spread bucket counts)
- 15 FVG retest watcher / hold quality counters
- 4 OnTradeTransaction routing counters (`g_ottu_*`)
- 6 FVG runtime quality-guard counters (`g_fvg_qguard_runtime_*`)
- 1 session-start balance (`g_falcon_session_start_balance`)
- 18 engine singletons (`g_market_context`, `g_broker_entry_bridge`, etc.)
- 4 runtime state primitives (`g_is_initialized`, `g_runtime_tick_counter`, `g_last_processed_m5_closed_candle_time`, `g_last_staged_fvg_candidate_id`)

### `Apply*` chain (in `CFalconReportWriter`, called from `RegisterClosedTrade`)
1. `ApplyFvgQualityShadowGuardSimulation`
2. `ApplyPaperRuntimeGuardApplication`
3. `ApplyPaperRuntimeSmartSLProtectionApplication`
4. `ApplyPaperRuntimeRunnerApplication`
5. `ApplyCapitalTierFoundation`
6. `ApplyLowCapitalRiskFeasibilityFoundation`
7. `ApplyDynamicLotSizingModel`
8. `ApplyCalibratedSingleTradeLossCapEnforcement`
9. `ApplyNoNewEntryUserOverridePaperEnforcement`
10. `ApplyDailyWeekendForceCloseUserOverridePaperEnforcement`
11. `ApplyThreeLayerEmergencyApplication`
12. `ApplyCapitalFlowSourceClassification`

---

## 7. Cross-version provenance summary

| Version | Headline change | Surface |
|---|---|---|
| v0.55.13-fix4 | Recovery blocked-entry state fix (LOCKED baseline) | Paper enforcement |
| v0.56.9b | Broker Execution Layer + Emergency Server SL/TP Envelope (restored dev base) | OrderSend entry + atomic SL/TP |
| v0.57.0 | Lock Parity Executability Decomposer (2 CSVs + totals struct, +458/−5) | Report-only |
| v0.57.1 | LOCKPARITY report profile + Runner Exit Price Probe (+182/−23) | Report-only, runner classification |
| v0.57.2 | Virtual Trailing Exit Bridge (Simple) — first exit-behavior change (+466/−88) | Tester-only managed close via reverse OrderSend |
| v0.57.3 | Trailing calibration (8/6/4) + spread-aware BE + USD column + filename tag fix (+49/−9) | Behavior fix + measurement |
| v0.57.4 | Structural Breakeven — anchor BE floor on last confirmed M5 swing (+124/−11) | BE floor calculation only; trailing stop untouched |

---

*End of inventory. No source modifications were made by this scan.*
