# JA FalconCore — ملفّ ذاكرة المشروع الشامل
## محدَّث حتى R2.1 (مُغلقة) — 31 مايو 2026

> **غرض هذا الملفّ:** المرجع الموثَّق لحالة المشروع. يُقرأ في بداية
> أيّ محادثة جديدة لاستئناف العمل فورًا. **يُحدَّث في نهاية كلّ
> مرحلة** — لا يُترك يتقادم.

---

## القسم الأوّل: ما هو المشروع

منصّة تداول آليّة (MT5 / MQL5). الهدف بعيد المدى: EA ذكيّ ومتين
يعيش سنين. الفلسفة الحاكمة — **«الدليل أوّلًا. التنفيذ أخيرًا.»**
لا تخمين لسبب أيّ مشكلة؛ تُطلب البيانات وتُحلَّل. الحدس أخطأ مرارًا
في هذا المشروع؛ البيانات لم تخطئ.

الأدوار: المستخدم يملك الرؤية، ويترجم المواصفات عبر Claude Code،
ويشغّل الاختبارات ويرفع التقارير. المخطِّط/المحلّل يكتب وثائق
المواصفات ويحلّل النتائج — لا يكتب الكود مباشرةً.

---

## القسم الثاني: الرحلة — ماذا أُنجِز بالترتيب

### تشخيص S00 (R1.2d) — مُغلق
S00 (استراتيجيّة FVG Micro) **بلا حافة**. ربحت في ربيع 2025 وحده
(+150$)، ثمّ 9 أشهر خسائر. الأداء الظاهر = ربعٌ محظوظ، لا حافة.
خطؤها القاتل: سلّحت الحماية مبكّرًا (+8 نقاط على M5). **أُرشِفت.**

### تقرير التحقّق — مُغلق
كشف أن طبقة `TradeManagement` كانت **اسمًا لا طبقة**: المحرّكات
السبعة المُدّعاة (حماية، runner، partial…) كلّها paper-only، حساب
بعد-الإغلاق — خيالٌ محاسبيّ، لا تعديل حيّ. الموجود حقيقيًّا: دخول
بروكر عبر Bridge مع SL/TP، خروج عبر server SL/TP.

### المرحلة 1 — العمود الفقريّ للتنفيذ (R1.3a) — مُغلقة
بناء `S01_Harness`: استراتيجيّة اختبار بدخولٍ حتميّ بسيط. **أثبتت
أن العمود الفقريّ يعمل end-to-end:** 585 صفقة نُفّذت حقيقيًّا على
البروكر. اكتشافٌ مهمّ خلالها: ملفّ `TestB_Failure_Analysis` كان
مغلوطًا — صُحِّح بـ`TestB_Diagnosis_Record`. وكُشفت ثلاثة عيوب:
(أ) مفتاح إطفاء legacy لا يوقف محاكاتها الظلّيّة؛ (ب) `FinalWorkingNetUSD`
الورقيّ يخالف البروكر؛ (ج) علامة Emergency BLOCKED منفصلة عن الواقع.

### المرحلة 2 — مقياس الحقيقة + طبقة التنسيق — مُغلقة
- **2a (R1.4a):** «مقياس الحقيقة التقريريّ». استكشافٌ أثبت أن
  `FinalWorkingNetUSD` الورقيّ ليس الحقيقة (طبقة emergency تُصفّره)؛
  الحقيقة جانب البروكر. أُضيف عمود `BrokerActualNetUSD` للـ Summary.
- **2b (R1.4b):** «منسّق إدارة الصفقة + واجهة المحرّكات». بُنيت
  واجهة `CFalconTradeEngine` المجرّدة + منسّق يُستدعى لكلّ شمعة M5
  مغلقة لكلّ مركز بروكر مفتوح، يبني السياق من حالة البروكر. أُثبت
  بمحرّك no-op (السلك يعمل، السلوك خامل).

### المرحلة 3a — محرّك الحماية (R1.5a + R1.5a-fix) — مُغلقة
بُني `ProofProtectionEngine` + مفصلة `ProtectionPolicy` (قابلة
لتبديل السياسات) + سياسة `FixedRMultipleProtection` + مسار تطبيق
القرار. تصحيح `R1.5a-fix`: السياق صار هجينًا — يحمل الوقف الهيكليّ
للاستراتيجيّة (لا مظروف البروكر) ليُحسب R صحيحًا.
**النتيجة:** المحرّك مُثبَتٌ **آليًّا** (يُسلّح، يُعدّل SL البروكر
فعلًا — أوّل تعديلٍ حيّ في تاريخ المشروع، حارس الاتّجاه الواحد
صامد). لكن مسح القيم على 4 أشهر أثبت أن **لا قيمة حماية تتفوّق على
OFF** على الـ harness — وهذا متوقَّع: S01 بلا حافة، والحماية لا
تصنع حافة. قيمة الحماية الربحيّة **مؤجَّلة لاستراتيجيّة حقيقيّة**.
`Enable_S01_Protection` يبقى `false` افتراضيًّا.

### R1.6a — تنظيف التقارير التشخيصيّة — **مُغلقة**
استكشاف `DeadSurfaceAudit` حصر بالدليل: 0 مدخلات ميّتة، نحو 24
تقريرًا تشخيصيًّا منتهيًا. مواصفة R1.6a حذفت نداءات كتابتها.
**اختبار التكافؤ الأوّل (الخاطئ):** اعتمد `FinalWorkingNetUSD`
بوّابةً — انحراف 42.60→1612.07 ⇒ علامة فشل. **الاستكشاف الـ
line-by-line:** R1.6a لمست **صفر أسطر** في كود الحماية/Risk/المنسّق —
حياد كاملٌ مُثبَتٌ بالـ diff. **إعادة الاختبار بالمعايير الصحيحة:**
`RawNetUSD = 1233.88` + `BrokerActualNetUSD ≈ 321.9` + `TotalTrades
= 1146` + `Reconciliation diffs ≈ 0` ⇒ كلّها مطابقة للـ baseline.
تكافؤ سلوكيّ مُثبَت ⇒ commit انعقد. **الانحراف الورقيّ تفسيره
خارجيّ:** (أ) تغيّر تعريف ضمنيّ في R1.5a (محرّك الحماية صار يُعدّل
الرقم الورقيّ)، (ب) عيب gate جديد مُسجَّل (Problem #6) في المفتاحين.

### R1.7 — إصلاح تسرّب الـ Gate في الطبقة الورقيّة — **مُغلقة**
استكشاف `Exploration_R1_7_GateLeakAudit.md` (line-by-line) حصر تسرّب
الـ gate في 3 ملفّات (~5-15 سطرًا). **المحاولة الأولى** (`Spec_R1_7_GateLeakFix.md`):
أربع تغييرات صحيحة المواضع، لكنّ قرار «تجاوزٌ كامل» (عدم استدعاء
Apply #5/#6 عند OFF) ترك حقول الطبقة الورقيّة **غير مُهيَّأة** ⇒
تشغيلَا v1/v2 أظهرا ذاكرة فاسدة (`FinalWorkingNetUSD = 5.38e+267`)
وعدّاداتٍ وهميّة (`ProtectionActivated = 467`) في Bare. **المُكمِّلة**
(`Spec_R1_7_Amend_DefensiveInit.md`): نُقل الحارس من موضع الاستدعاء
إلى **داخل** Apply #5/#6 بعد كتلة التهيئة الدفاعيّة — التهيئة
passthrough تشتغل دائمًا، المنطق محروس. **تشغيلَا v3: PASS** — Bare
0/0/−2.43، ON 2/2/17.53. الإغلاق commit واحد يجمع المحاولتين.
**يُغلق Defect #4 و#6.** الدرس مُسجَّل (معرفة #10).

### R2.1 — إكمال FvgMicroRetest وفق شرح الاستراتيجيّة — **مُغلقة**
**التنفيذ:** سبعة تعديلات تُحاذي FvgMicroRetest مع شرحها الأصليّ —
مرتكز TP (حدّ FVG → midpoint)، Invalidation guard (إغلاق متجاوز
يُبطل المنطقة)، Spread guard نسبيّ (`spread/zoneSize ≤ 0.50`)، SL
buffer قابل للضبط (`FvgMicroSlBufferPoints`، افتراضي 30)، Age guard
فعّال (max 12 شمعة على M5)، Retest confirmation كبوّابة اختياريّة،
و Min size `1.00 → 30.0` (placeholder للمعايرة بعد R2.2). اكتشاف Q3
retest-confirmation اللاحق احتُوي **محليًّا داخل Watcher بدون تعديل
snapshot** (الخيار الثالث).
**التحقّق:** 5 تشغيلاتٍ تحقّقيّة. الحارس يعمل (Test C: Protection=0،
Runner=0)، شاشة Tester ≈ EA report، Test B يطابق R1.7 SanityON بالضبط
(482.64).
**الاكتشاف عن التركيب:** «التراجع المُتوهَّم» (528.59→263.66) كان
مقارنة برتقال بتفّاح. R2.1 على إعداداتٍ pre-R1.7 موحَّدة = **614.45**
(أعلى بـ 86 دولارًا، لا أدنى). والتركيب يضيف قيمة: A−(B+C)=**+77.74**؛
TM/Protection يتدخّل 515/225 مرّة في الإعداد الكامل مقابل 0 في
FvgMicro وحدها. **الإغلاق commit واحد** على `refactor`. الدروس مُسجَّلة
(معرفتَا #11 و#12). المراجعة في `Docs/ReviewNotes/R2_1_Review_Notes.md`.

---

## القسم الثالث: الحالة الحاليّة (31 مايو 2026)

- **آخر commit مستقرّ:** **R2.1** — إكمال FvgMicroRetest وفق شرح
  الاستراتيجيّة (7 تعديلات spec-aligned + Q3 retest-confirmation fix
  داخل Watcher)، مُغلق بـ commit واحد على فرع `refactor` بعد 5
  تشغيلاتٍ تحقّقيّة.
- **الفرع:** `refactor`.
- **الخطوة التالية:** **R2.3 (بوّابة الحافة)** ثمّ **R2.2 (تشغيل 3
  أشهر)**. معايرة أيّ عتبة قبل بيانات R2.2 = data mining (Backlog #93).

---

## القسم الرابع: خريطة البناء — 4 مراحل

| المرحلة | المحتوى | الحالة |
|---|---|---|
| 1 | الـ Harness + العمود الفقريّ للتنفيذ | ✓ مُغلقة (R1.3a) |
| 2 | مقياس الحقيقة + منسّق إدارة الصفقة + الواجهة | ✓ مُغلقة (R1.4a/R1.4b) |
| 3a | محرّك الحماية | ✓ مُغلقة (R1.5a/-fix) |
| تنظيف | حذف التقارير التشخيصيّة المنتهية | ✓ مُغلقة (R1.6a) |
| إصلاح gate | إطفاء الطبقة الورقيّة + عقم legacy shadow | ✓ مُغلقة (R1.7) |
| استراتيجيّة | **إكمال FvgMicroRetest وفق الشرح** | **✓ مُغلقة (R2.1)** |
| **التالي** | **R2.3 (بوّابة الحافة) ثمّ R2.2 (3 أشهر)** | لم تبدأ |
| 3b/3c… | محرّكات إضافيّة (runner، partial…) — **بعد** استراتيجيّة ذات حافة مُثبَتة | مؤجَّلة |
| 4 | جعل الحوكمة حقيقيّة (force-close، KillSwitch فعليّ) | لم تبدأ |

**قرار استراتيجيّ مُثبَت:** قيمة أيّ محرّك لا تُقاس على الـ harness
(بلا حافة) — فقط آليّته. لذا المحرّكات الإضافيّة (runner…) تنتظر
استراتيجيّةً حقيقيّة نقيس عليها. المحرّكات خدمٌ للاستراتيجيّات.

---

## القسم الخامس: المعارف المُثبَتة (قواعد لا تُنقَض)

1. **التقارير الورقيّة تكذب.** `FinalWorkingNetUSD` رقمٌ مسموم
   (طبقة emergency تُصفّره، ومحرّك الحماية يُعدّله منذ R1.5a).
   **مقياس الحقيقة المُعتمَد = `BrokerActualNetUSD`** (من
   `BrokerPaperTradeReconciliation`، مصالَحًا نحو صافي MT5).
   **توسيع R1.6a-closed:** الـ kill-switches قد تكون **decorative** —
   يجب التحقّق من فاعليّة الـ gate قبل الاعتماد عليه (Problem #6 كان
   المثال الحيّ، وقد أُصلح في R1.7).
2. **لا حماية مبكّرة.** خطأ S00 القاتل. عتبة تسليح الحماية ≥ 1.0R
   حصرًا — حارسٌ بنيويّ.
3. **كلّ تغيير استراتيجيّ يُختبَر على 3 أشهر** (تشغيل ممتدّ،
   تقطيع شهريّ في التحليل) — ضدّ الـ overfitting.
4. **لا تُختَر قيمة من شهرٍ محظوظ** — تُختار المستقرّة عبر الأشهر.
5. **سياق المحرّك هجين:** البروكر مصدر الحقيقة للمُخرَجات،
   الاستراتيجيّة مصدر المخاطرة المقصودة (الوقف الهيكليّ).
6. **commit واحد لكلّ مرحلة، إن نجح الاختبار.** لا commit على
   بناءٍ نظيف وحده — «يُبنى» ≠ «يعمل».
7. **التحقّق يبدأ بإعادة بناء صريحة (F7)** — لا ثقة ببناء قديم؛
   تأكّد أن إصدار التقارير يطابق المرحلة.
8. **ترتيب سلسلة الـ Apply\*:** `#3،#4،#5،#6،#7،#8،#10،#9،#1،#2،#11،#12`
   — ليس تصاعديًّا. لا يُمسّ.
9. لكلّ مدخل مبرّر؛ التعليق الظاهر بعد `//` وصفيٌّ نظيف بلا أرقام
   مراحل.
10. **الـ gates داخليّة:** حارسٌ على دالّة init-aware يكون **داخلها
    بعد التهيئة الدفاعيّة**، لا حولها — وإلّا تتسرّب ذاكرة غير
    مُهيَّأة إلى downstream readers. (الدرس المُستخلَص من R1.7:
    تجاوز Apply #5/#6 بالكامل ترك `paper_protection_*`/`paper_runner_*`
    ذاكرةً عشوائيّة ⇒ 467 عدّادًا وهميًّا و5.38e+267. الحلّ: التهيئة
    دائمًا تشتغل، `if(!flag) return;` بعدها.) أيّ مواصفة gating
    مستقبليّة تخضع لهذه القاعدة.
11. **حدسٌ بالتراجع يستحقّ تحقّقًا.** ليس نقاشًا. التحقّق = 3
    تشغيلاتٍ بإعداداتٍ متطابقة على نفس النافذة. مقارنة الإعدادات
    السابقة بالحاليّة دون توحيد الإعدادات = برتقال بتفّاح. (الدرس
    من R2.1: «التراجع المُتوهَّم» 528.59→263.66 كان خطأ مقارنة؛
    التوحيد أثبت تحسّنًا بـ 86 دولارًا.)
12. **التركيب يضيف قيمة.** Trade Management (Apply #5/#6) يفعّل
    515/225 مرّة في الإعداد الكامل (Test A) مقابل 0 في FvgMicroRetest
    وحدها (Test C). الفائض A−(B+C) = +77.74 USD. الإدارة ليست زخرفًا.

---

## القسم السادس: الأرقام المرجعيّة

### الـ baseline الإنتاجيّ المرجعيّ على R2.1 (April 2026, US100_Spot M5)

**Test A — الإعداد الكامل (S01 + FvgMicro + TM/Prot ON):**

| المقياس | Test A (الكامل) |
|---|---:|
| TotalTrades | 1168 |
| RawNetUSD | 1194.23 |
| FinalWorkingNetUSD | 1264.42 |
| **BrokerActualNetUSD** | **417.37** |
| Tester Net Profit | 414.45 |
| Final Balance (200 ابتدائيّ) | **614.45** |
| ProtectionActivated | 515 |
| RunnerActivated | 225 |
| WinRate | 50.00% |
| Balance DD Absolute | 5.03 |
| Equity DD Absolute | 6.25 |

**الـ baseline العاري لـ FvgMicroRetest وحدها (Test C، نفس النافذة):**
558 صفقة، Broker 63.66، Final Balance 263.66، WR 63.44%،
Equity DD 97.51 (كبير — بدون TM).

**Test B (S01 + TM/Prot ON ≡ R1.7 SanityON):** 482.64 / Broker 275.97
— نقطة لمسٍ مرجعيّة تطابق R1.7 بالضبط.

> ملفّات الـ `.set` للتشغيلات الثلاثة محفوظة في `Docs/SetFiles/R2_1/`:
> `R2_1_TestA_PreR17_Reproduce.set`، `R2_1_TestC_FvgMicroOnly_Baseline.set`،
> `R2_1_TestB_R17_SanityON_Reproduce.set`.

### الـ baseline التاريخيّة (تبقى مراجع لمراحلها لا لـ R2.1)

- **Bare Baseline على بناء R1.7 (أبريل 2026، US100_Spot M5):** 610
  صفقة، `RawNetUSD = 648.71`، `BrokerActualNetUSD = 275.97`،
  `FinalWorkingNetUSD = −2.43`، `ProtectionActivated = 0`، `Runner = 0`.
  **Sanity ON:** `ProtectionActivated = 2`، `Runner = 2`،
  `FinalWorkingNetUSD = 17.53`. **يُحفظ كمرجعٍ لـ R1.7 نفسها لا لـ R2.1.**
- **مسح الحماية 4 أشهر** (فبراير–مايو، إيداع 1000$،
  `BrokerActualNetUSD`): OFF −145.07، trig1.5 −161.30،
  trig2.0 −223.61. لا قيمة تتفوّق على OFF.

### أرقام تاريخيّة متقاعدة

- **`FinalWorkingNetUSD = 42.60`** (تشغيل S01 أبريل، الرقم الورقيّ):
  **يبقى متقاعدًا.** ينتمي لحقبة ما قبل R1.5a — قبل أن يُعدّل محرّك
  الحماية الرقم الورقيّ — فلا يصلح مرجعًا لبنى R1.5a+.
- **أساس regression — أبريل legacy:** 536 صفقة، `RawNetUSD = 585.17`،
  `FinalWorkingNetUSD = 1104.89` (حقبة pre-R1.5a — لا يصلح مرجعًا
  لبنى R1.5a+).

---

## القسم السابع: العيوب المعروفة

### مُغلقة، مُحلَّلة بـ R1.7

- **#4 — `EnableStrategy_FvgMicroRetest = false` لا يوقف legacy shadow.**
  **مُحلَّل بـ R1.7.** الـ Registry (`Router/FalconStrategyRegistry.mqh:72`)
  والـ Legacy Shadow Pipeline (`mq5:5806`) صارا يقرآن المفتاح فعلًا
  (أُزيل الـ OR مع `#define EnableFvgMicroRuntimePipelineRefresh`).
  أُثبت بـ Bare: التوقّف خفّض الصفقات من 1146 إلى 610.
- **#6 — `Enable_TradeManagement` / `Enable_S01_Protection` لا يطفئان
  الطبقة الورقيّة.** **مُحلَّل بـ R1.7.** حارسٌ داخليّ في Apply #5
  (`!Enable_TradeManagement || !Enable_S01_Protection`) و#6
  (`!Enable_TradeManagement`) بعد التهيئة الدفاعيّة. أُثبت بـ Bare:
  `ProtectionActivated = 0`، `Runner = 0`، ولا ذاكرة فاسدة.

### مُغلقة سابقًا

- **#1 — R1.6a `FinalWorkingNetUSD` انحرف** (42.60 → 1612.07).
  **مُغلق في R1.6a-closeout** — ليس انحدارًا؛ 0 أسطر مُمسّت في كود
  الحماية/Risk/المنسّق. السبب خارجيّ: تغيّر تعريف ضمنيّ في R1.5a +
  Problem #6 (المُصلَح الآن في R1.7).

### مفتوحة (بلا تغيير في R2.1 — غير حاجبة لـ R2.2)

- **#2 — `BrokerModifySent` / `RuntimeSLChanged` = 0** رغم تعديلات SL
  حقيقيّة من المنسّق — عدّادات الـ Summary عمياء عن مسار المنسّق.
  يُوصَل قبل محرّكات تُصدر أفعالًا. Backlog #80.
- **#3 — `ReportIntegrityStatus = FAIL`** — إيجابيّة كاذبة #69
  (بوّابة `EnableTierEmergencyReports`). معروفة، **غير حاجبة**.
  Backlog #69.
- **#5 — عمود `TP2`** في الـ Reconciliation غير مُهيَّأ (قيم عشوائيّة)
  — يكسر حتميّة بايتيّة للتقرير. **غير حاجب.**

---

## القسم الثامن: أفكار مؤجَّلة (Ideas Backlog — مختصر)

> القائمة الكاملة في `Docs/Ideas_Backlog.md`. أبرز ما أُضيف في R2.1:
- **#93** معايرة العتبات الأربعة (`MIN_SIZE_POINTS`، `MAX_SPREAD_TO_FVG_RATIO`،
  `MAX_FVG_AGE_BARS`، `FvgMicroSlBufferPoints`) — تُؤجَّل حتى بيانات R2.2
  (3 أشهر). أيّ معايرة قبلها = data mining.
- **#94** `R2.2-StrategyOnly` (FvgMicroRetest وحدها على 3 أشهر) — مؤجَّل،
  سؤالٌ علميّ مفيد لكن خارج المسار الإنتاجيّ.
- **#95** Protection/Runner على البروكر = R3.x (مظروف الطوارئ يبقى حتى ذلك).
- **#96** `FalconStrategySignal` refactor — اتّساق معماريّ، غير حاجب.
- **#97** ازدواج مسار الدخول OnInit/OnTick — مراجعة `g_last_staged_fvg_candidate_id`
  (read-only).
- (سابقة R1.7) #88 الحارس داخل الدالّة؛ #89 Apply #4 يقصّ الـ Raw؛
  #90 مراجعة بقيّة Apply\*؛ #91 حذف `#define EnableFvgMicroRuntimePipelineRefresh`؛
  #92 `.gitignore` لـ `.vscode/temp/` و `*.log`.

---

## القسم التاسع: كيفية الاستمرار

1. **اقرأ هذا الملفّ** = briefing فوريّ.
2. **الأولويّة الحاليّة:** R2.3 (بوّابة الحافة) ثمّ R2.2 (تشغيل 3
   أشهر). R2.1 مُغلقة. لا معايرة عتبات قبل بيانات R2.2.
3. الملفّات: المواصفات والاستكشافات في جذر المستودع و`Docs/Explorations/`؛
   ملفّات الـ `.set` المرجعيّة في `Docs/SetFiles/R2_1/`.
4. تقارير الـ Strategy Tester على جهاز المستخدم:
   `C:\Users\jalak\AppData\Roaming\MetaQuotes\Terminal\Common\Files`.

---

## سجلّ الإصدارات

| الإصدار | المحتوى | الحالة |
|---|---|---|
| R1.2d | أساس — S00 مُشخَّصة | مُغلق |
| R1.3a | S01 Harness — العمود الفقريّ | مُغلق |
| R1.4a | مقياس الحقيقة التقريريّ | مُغلق |
| R1.4b | منسّق إدارة الصفقة + الواجهة | مُغلق |
| R1.5a | محرّك الحماية | مُغلق |
| R1.5a-fix | الوقف الهيكليّ في السياق | مُغلق |
| R1.6a | تنظيف التقارير التشخيصيّة المنتهية | مُغلق |
| R1.7 | إصلاح تسرّب الـ Gate في الطبقة الورقيّة | مُغلق |
| R2.1 | إكمال FvgMicroRetest وفق شرح الاستراتيجيّة | **مُغلق** |

---

**كُتب:** 31 مايو 2026 | **الحالة:** R2.1 مُغلقة؛ الخطوة التالية =
R2.3 (بوّابة الحافة) ثمّ R2.2 (تشغيل 3 أشهر).
