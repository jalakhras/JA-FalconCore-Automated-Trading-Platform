# JA FalconCore — R0.8c
## إعادة الهيكلة — تقليل الـ globals (بالتصنيف)
### مواصفات للتسليم إلى Claude Code

> **الهدف:** تقليل سطح الـ globals (`g_*`) — لكن **بالتصنيف ثم الإزالة
> الآمنة فقط**، لا بمطاردة رقم.
> **الأساس:** فرع `refactor`، بعد R0.8b.

---

## 0. المبدأ — التصنيف قبل الإزالة

خطة إعادة الهيكلة ذكرت هدفًا: globals من 85 إلى <20. لكن **الرقم ليس
الهدف — نظافة التصنيف هي الهدف.** الـ globals متشابكة؛ إزالة عمياء
لمطاردة رقم = خطر كسر سلوك.

R0.8c على مرحلتين داخليّتين:
1. **تصنيف** كل الـ `g_*` إلى ثلاث فئات.
2. **إزالة آمنة** للفئتين الآمنتين فقط. الفئة الثالثة تبقى.

النتيجة: العدد ينخفض **كأثر** للتنظيف الصحيح، لا كغاية بحدّ ذاتها.

---

## 1. الخطوة الأولى — التصنيف

افحص كل `g_*` في `JA_FalconCore_Automated_Trading_Platform.mq5` وصنّفه:

### فئة (أ) — تبقى: الكائنات الأساسية (layer singletons)
الكائنات العامّة الكبرى التي تمثّل طبقات المشروع:
`g_report_writer`، `g_risk_lifecycle_processor`، `g_broker_entry_bridge`،
`g_shadow_executor`، `g_runtime_safety_guard`، `g_strategy_registry`،
وأمثالها. **هذه تبقى** — هي بنية تحتية، ونمط الـ forwarder يعتمد عليها.
نقلها مشروع كبير مستقلّ، ليس ضمن R0.8c.

### فئة (ب) — تُزال: globals ميتة
`g_*` معرَّفة لكن **لا تُستخدَم في أي مكان** (صفر قراءة، صفر كتابة بعد
التعريف). تُحذف.

### فئة (ج) — تُحوَّل لمحلّية: globals بنطاق دالة واحدة
`g_*` تُقرأ وتُكتَب **داخل دالة واحدة فقط** — لا داعي لكونها عامّة. تُحوَّل
إلى متغيّر محلّي (`static` داخل الدالة إن كانت تحتاج البقاء بين النداءات).

> أي `g_*` لا يقع بوضوح في (ب) أو (ج) → يبقى في (أ) افتراضيًّا. عند الشكّ،
> الإبقاء أأمن.

**أخرِج التصنيف أولًا** في `Docs/ReviewNotes/R0_8c_Review_Notes.md` —
جدول: كل `g_*`، فئته، وسبب التصنيف. هذا قبل أي حذف.

---

## 2. الخطوة الثانية — الإزالة الآمنة

بعد التصنيف، نفّذ على الفئتين (ب) و(ج) فقط:
- **(ب) ميتة:** احذف التعريف. تأكّد بـ grep أنها فعلًا صفر استخدام.
- **(ج) محلّية:** انقل التعريف إلى داخل دالتها الوحيدة (`static` إن لزم
  البقاء بين النداءات — حافِظ على دلالة البقاء إن كانت موجودة).

**فئة (أ) لا تُمَسّ إطلاقًا.**

---

## 3. القيود الصارمة

- **صفر تغيير سلوك.** بعد R0.8c، FixedLot April يعطي
  `585.17 / 1104.89 / 157.49` بالضبط.
- لا تمسّ الكائنات الأساسية (فئة أ) — خاصّةً `g_risk_lifecycle_processor`
  (نمط الـ forwarder يعتمد عليه) و`g_report_writer`.
- لا تغيّر ترتيب التهيئة في `OnInit` — `g_risk_lifecycle_processor.Initialize`
  يجب أن يبقى قبل أي استدعاء تقارير يستخدم `SymbolContext()`.
- تحويل global إلى `static` محلّي يجب أن يحافظ على دلالة "البقاء بين
  النداءات" إن كانت موجودة — لا تكسر منطقًا يعتمد على قيمة محفوظة.
- لا تعديل منطق، لا لمس سلسلة الـ Apply\*، لا لمس التقارير.
- إن كان تصنيف global غامضًا — أبقِه في (أ) وسجّل الغموض، لا تُجبر حذفًا.

---

## 4. تحديث `EA_VERSION_TAG`

في `Core/FalconConstants.mqh`، حدّث `EA_VERSION_TAG` إلى `R0_8c`.

---

## 5. بروتوكول العمل (بنود ثابتة)

1. راجع نافذة Problems في VS Code قبل التسليم؛ ترجمة F7 الحَكَم.
2. أفكار التحسين → `Docs/Ideas_Backlog.md`، لا تُنفَّذ.
3. اكتب `Docs/ReviewNotes/R0_8c_Review_Notes.md` — يبدأ بجدول التصنيف.

---

## 6. معايير القبول

- `Docs/ReviewNotes/R0_8c_Review_Notes.md` يحوي جدول تصنيف كامل لكل
  `g_*` (فئة + سبب).
- globals الفئة (ب) محذوفة؛ الفئة (ج) محوّلة لمحلّية.
- الفئة (أ) — الكائنات الأساسية — لم تُمَسّ.
- عدد الـ `g_*` انخفض (كأثر للتنظيف) — أبلِغ بالرقم قبل/بعد.
- `EA_VERSION_TAG` = `R0_8c`.
- يُترجَم: `0 errors, 0 warnings`.
- تشغيل FixedLot April يعطي `585.17 / 1104.89 / 157.49` — مطابق تمامًا.

---

## 7. ما يجب ألا يحدث

- لا حذف/نقل لأي كائن أساسي (فئة أ).
- لا تغيير سلوك. Summary + البروكر يطابقان.
- لا تغيير ترتيب تهيئة `OnInit`.
- لا حذف global قبل إثبات أنه فئة (ب) أو (ج) بـ grep.
- لا مطاردة رقم على حساب الأمان.

---

## 8. بعد R0.8c

عند نجاح الترجمة + تطابق الأرقام → **commit واحد**:
```
git add -A
git commit -m "R0.8c - globals reduction: remove dead globals, localize single-function globals"
```
بهذا تكتمل R0.8. ثم R0.9 (التثبيت النهائي — وبه تكتمل إعادة الهيكلة).

---

## مُلحَق — Prompt لبدء العمل في Claude Code

> فعّل وضع accept edits / auto mode أولًا.
> اقرأ `JA_FalconCore_R0_8c_SPEC.md` ونفّذه على خطوتين: (1) **صنّف** كل
> `g_*` في الملف الرئيسي إلى ثلاث فئات — (أ) كائنات أساسية تبقى، (ب) ميتة
> غير مستخدمة، (ج) بنطاق دالة واحدة. أخرِج جدول التصنيف في
> `Docs/ReviewNotes/R0_8c_Review_Notes.md` أولًا. (2) **أزِل بأمان**: احذف
> الفئة (ب)، وحوّل الفئة (ج) إلى متغيّرات محلّية (`static` إن لزم البقاء
> بين النداءات). **لا تمسّ الفئة (أ)** — خاصّةً `g_risk_lifecycle_processor`
> و`g_report_writer`. لا تغيّر ترتيب تهيئة `OnInit`. لا تغيّر سلوكًا. عند
> الشكّ في تصنيف، أبقِ الـ global في (أ). حدّث `EA_VERSION_TAG` إلى
> `R0_8c`. راجع نافذة Problems. سجّل أفكار التحسين في `Docs/Ideas_Backlog.md`.
> اجعله commit واحد. بعد الانتهاء أكّد git diff --stat وأبلِغ بعدد الـ
> `g_*` قبل وبعد.
