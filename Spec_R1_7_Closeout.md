# مواصفة إغلاق R1.7

> **النوع:** إغلاق مرحلة — commit واحد + تحديث وثائق.
> **الإصدار:** R1.7 / GateLeakFix (هذا commit الإغلاق).
> **يسبقه:** R1.6a (commit `24b19ed`) + Spec_R1_7_GateLeakFix.md +
> Spec_R1_7_Amend_DefensiveInit.md + Exploration_R1_7_GateLeakAudit.md.

---

## ١. الهدف

إغلاق R1.7 بـ commit واحد على فرع `refactor` يجمع:
- التغييرات الأربعة الأصليّة (R1.7).
- التغييرات الثلاثة المُكمِّلة (Amend — نقل الحارس داخل Apply #5/#6).

مع تحديثٍ كاملٍ للـ Review Notes والذاكرة وقائمة Problems، وتسجيل الـ
baseline العاري المرجعيّ الجديد.

---

## ٢. السياق الموجز

| الدليل | الخلاصة |
|---|---|
| الاستكشاف (line-by-line) | تسرّب الـ gate في ٣ ملفّات، ~5-15 سطرًا |
| R1.7 الأصلي | إصلاحٌ صحيحٌ في 4 مواضع، لكن «تجاوز كامل» ترك حقولًا غير مُهيَّأة |
| تشغيلَا v1/v2 | كشفا ذاكرة فاسدة (5e+267) وعدّاداتٍ وهميّة (467) |
| المُكمِّلة (Amend) | نقل الحارس داخل Apply بعد التهيئة الدفاعيّة |
| تشغيلَا v3 | **PASS** — Bare: 0/0/−2.43، ON: 2/2/17.53 |

**الدرس المُستخلَص:** الحارس على دالّة init-aware يكون **داخلها بعد
التهيئة** لا حولها. أُسجَّل قاعدة عمل.

---

## ٣. القيود الصارمة

- **لا تغيير كود إضافيّ** في هذا الـ commit. ما في الـ working copy حاليًّا
  (R1.7 + Amend) هو المُلتزَم.
- **commit واحد فقط**. لا commit ثانٍ لـ logs أو غيره.
- **لا تنظيف إضافيّ ولا تحسينات** — أيّ فكرة → Backlog.
- بعد commit: لا تشغيلات إضافيّة، لا تجارب.

---

## ٤. التحديثات المطلوبة قبل الـ commit

### أ. الـ Review Notes — `Docs/ReviewNotes/R1_7_Review_Notes.md` (جديد)

- ملخّص التنفيذ على مرحلتَين (الأصليّة + Amend).
- معايير القبول النهائيّة + قيمها المُحقَّقة من v3.
- إشارة للاستكشاف والمواصفتَين الأصليّتَين.
- الـ baseline العاري المرجعيّ مع جدوله.
- درس مكتسَب: حارس على init-aware functions = داخل، لا حول.

### ب. قائمة Problems — يُغلق #4 و#6

- **#4** (`EnableStrategy_FvgMicroRetest=false` لا يوقف legacy shadow) ⇒
  **مُحلَّل بـ R1.7** (Registry + Pipeline يقرآن المفتاح فعلًا الآن).
- **#6** (`Enable_TradeManagement`/`Enable_S01_Protection` لا يطفئان طبقة
  الورق) ⇒ **مُحلَّل بـ R1.7** (حارس داخليّ في Apply #5 و#6 بعد التهيئة).

### ج. `Ideas_Backlog` — يُضاف

- **قاعدة عمل جديدة:** «الحارس على دالّة init-aware يكون داخلها بعد
  التهيئة، لا حولها. أيّ مواصفة gating مستقبليّة تخضع لهذه القاعدة».
- **ملاحظة Apply #4 (guard):** يقصّ الـ Raw 648.71 إلى −2.43 (paper-only،
  البروكر مستقرّ 275.97). قابلٌ لمراجعةٍ حين تُقاس قيمة الـ guard على
  استراتيجيّةٍ ذات حافة.
- **مراجعة بقيّة سلسلة Apply\*:** هل تتبع نمط init-then-logic؟ إن نعم،
  نفس استراتيجيّة الحراسة قابلةٌ للتطبيق عند الحاجة.
- **حذف `#define EnableFvgMicroRuntimePipelineRefresh`** بعد التأكّد من
  عدم استعماله (تنظيف لاحق).
- **`.gitignore`:** أُضِف `.vscode/temp/` و`*.log` (تذكير من R1.6a).

### د. ملفّ ذاكرة المشروع — إصدار جديد `R1_7_closed`

- **§٢:** أضف فقرة عن إغلاق R1.7 (الاستكشاف، الأصليّة، Amend، التشغيلات v3).
- **§٣ (الحالة):** آخر commit مستقرّ = R1.7. الفرع `refactor`. الخطوة
  التالية = حسب المخطّط الشامل (R1.8 أو R2.0 — قرار للمناقشة).
- **§٤:** R1.7 ⇒ **✓ مُغلقة**.
- **§٥ (المعارف):** أضف معرفة جديدة:
  > **#10 — الـ gates داخليّة:** حارسٌ على دالّة init-aware يكون
  > **داخلها بعد التهيئة الدفاعيّة**، لا حولها — وإلّا تتسرّب ذاكرة
  > غير مُهيَّأة إلى downstream readers.
- **§٦ (الأرقام المرجعيّة):** أضف الـ baseline العاري الجديد:
  > **Bare Baseline على بناء R1.7 (April 2026, US100_Spot M5):**
  > 610 صفقة، RawNetUSD 648.71، BrokerActualNetUSD 275.97،
  > FinalWorkingNetUSD −2.43. الرقم −2.43 أثرٌ من Apply #4 (guard)
  > على الطبقة الورقيّة وحدها.
  > **رقم 42.60 يُنقَل إلى «أرقام تاريخيّة متقاعدة».**
- **§٧ (Problems):** انقل #4 و#6 إلى قسم «مُغلقة».
- **§٩:** الأولويّة = القرار التالي حسب المخطّط الشامل.

---

## ٥. الـ commit

- الفرع: `refactor`.
- المحتوى: كلّ تغييرات الـ working copy الحاليّة (R1.7 + Amend).
- النصّ المقترح:

  ```
  R1.7 — إصلاح تسرّب الـ Gate في الطبقة الورقيّة

  - حارس Apply #5 و#6: داخل الدالّتَين بعد التهيئة الدفاعيّة،
    على Enable_TradeManagement (Apply #6) و
    Enable_TradeManagement && Enable_S01_Protection (Apply #5).
  - EnableStrategy_FvgMicroRetest يحرس الـ Registry والـ Legacy
    Shadow Pipeline (إنهاء عقم OR مع #define = true).
  - يُغلق Defect #4 و#6.

  Bare Baseline الجديد (April 2026):
    TotalTrades=610, RawNetUSD=648.71, BrokerActualNetUSD=275.97,
    FinalWorkingNetUSD=-2.43, ProtectionActivated=0, Runner=0.
  Sanity ON: نفسها مع ProtectionActivated=2, Runner=2, Final=17.53.

  درس مُسجَّل (معرفة #10): حارس على دالّة init-aware = داخلها
  بعد التهيئة، لا حولها.

  رقم 42.60 يتقاعد رسميًّا. الـ baseline أعلاه هو المرجع الجديد.

  EA_VERSION_TAG: R1_7 / GateLeakFix
  ```

---

## ٦. معايير القبول

- `git log -1 --oneline` على `refactor` يُظهر سطر R1.7.
- `Docs/ReviewNotes/R1_7_Review_Notes.md` موجود ومُكتمل.
- Problems: #4 و#6 منقولان إلى «مُغلقة».
- `Ideas_Backlog`: البنود الخمسة مُضافة.
- ملفّ الذاكرة: إصدار `R1_7_closed` موجود مع التحديثات الستّة في §٤-د.
- لا تغيير في كود `.mq5/.mqh` غير ما هو موجود سلفًا في الـ working copy.
- لا commit ثانٍ.

---

## ٧. بروتوكول الإغلاق (قاعدة #٨)

- **مراجعة Problems:**
  - **مُحلَّل:** #4، #6.
  - **مفتوح:** #2 (`BrokerModifySent=0` — مرحلة R1.8 إن قرّرنا)، #3
    (`ReportIntegrityStatus=FAIL` إيجابيّة كاذبة #69 — غير حاجب)، #5
    (عمود `TP2` غير مُهيَّأ — غير حاجب).
- **تسجيل الأفكار:** §٤-ج أعلاه.
- **ملفّ المراجعة:** `R1_7_Review_Notes.md` (جديد).

---

## ٨. ملحق — Prompt لـ Claude Code

```
السياق: نحن على فرع `refactor`، آخر commit R1.6a (24b19ed). الـ working
copy يحوي تغييرات R1.7 الأصليّة + الـ Amend (مُحقّقة بنجاح في تشغيلَي v3).
نريد إغلاق R1.7 الآن بـ commit واحد + تحديثات وثائق.

التعليمات بهذا الترتيب الحرفيّ:

١. اقرأ كاملًا الملفّ المرفق `Spec_R1_7_Closeout.md`. هو المرجع الوحيد.
   كلّ ما يلي تعليمات تشغيليّة، ليست بديلًا عن قراءته.

٢. التزم بـ §٣ القيود (لا تغيير كود، commit واحد، لا تنظيف، لا تشغيلات).

٣. لا تلمس أيّ ملفّ .mq5/.mqh. تغييرات R1.7 + Amend تبقى كما هي.

٤. أنشئ `Docs/ReviewNotes/R1_7_Review_Notes.md` وفق §٤-أ. ضع:
   - ملخّص التنفيذ على مرحلتَين.
   - معايير القبول وقيمها المُحقَّقة (Bare 0/0/-2.43، ON 2/2/17.53).
   - مرجع للاستكشاف والمواصفتَين.
   - الـ baseline العاري المرجعيّ.
   - الدرس المُكتسَب (معرفة #10).

٥. حدّث Problems list (في ملفّ الذاكرة الحاليّ §٧ أو حيث تُسجَّل):
   انقل #4 و#6 إلى قسم «مُغلقة، مُحلَّل بـ R1.7».

٦. أضف إلى Ideas_Backlog (في الموقع المُعتمَد):
   - معرفة #10: حارس داخل دالّة init-aware لا حولها.
   - ملاحظة Apply #4 (guard) يقصّ Raw إلى -2.43.
   - مراجعة بقيّة Apply* للنمط init-then-logic.
   - حذف #define EnableFvgMicroRuntimePipelineRefresh لاحقًا.
   - .gitignore: .vscode/temp/ و *.log.

٧. أنشئ نسخة جديدة من ملفّ ذاكرة المشروع باسم
   `JA_FalconCore_Project_Memory_R1_7_closed.md` بالتحديثات في §٤-د.

٨. نفّذ commit واحدًا على `refactor` بالنصّ الكامل من §٥ (انسخه كما هو).

٩. توقّف. أظهر: git status، git log -1 --oneline، git diff HEAD~1 --stat.

ممنوع: أيّ تغيير كود، أيّ commit ثانٍ، أيّ تنظيف إضافيّ، أيّ بناء، أيّ
تشغيل اختبار، أيّ git rebase أو reset. ما لم يُذكر هنا — لا تفعله.

عند أيّ تعارض: المواصفة تحكم. عند أيّ غموض: اسأل قبل التصرّف.
```
