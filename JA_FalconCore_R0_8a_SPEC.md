# JA FalconCore — R0.8a
## إعادة الهيكلة — تنظيف آمن (ملفات + توثيق)
### مواصفات للتسليم إلى Claude Code

> **الهدف:** تنظيف منخفض المخاطر — تصحيح توثيق، حذف ملفات/تعليقات مؤقّتة،
> أرشفة. **لا تغيير منطق، لا تغيير بنية كود.**
> **الأساس:** فرع `refactor`، بعد R0.7cd.

---

## 0. لماذا R0.8a — ونطاقها

R0.8 (التنظيف) قُسّمت ثلاثًا:
- **R0.8a (هذه)** — تنظيف آمن: توثيق، ملفات، تعليقات، أرشفة. صفر مخاطرة.
- R0.8b — معالجة الحالة المكرّرة scaffolding (تغيير معماري — لاحقًا).
- R0.8c — تقليل الـ globals (عمل متشابك — لاحقًا).

R0.8a لا تمسّ منطقًا ولا بنية كود. كلّها تصحيح نصوص وحذف ملفات مؤقّتة.
لذلك مخاطرتها شبه معدومة — لكن نتحقّق بالترجمة والتشغيل كالعادة.

---

## 1. المهامّ

### 1.1 تصحيح توثيق ترتيب السلسلة (فكرة Backlog #1)
في `Docs/ReviewNotes/R0_6a_Review_Notes.md` §3.5، فقرة "caller graph"
تقول إن ترتيب سلسلة الـ Apply\* تصاعدي `#1..#12`. **هذا خطأ.**
الترتيب الفعلي (الموثّق في `ApplyTradeLifecycleChain`):
```
#3, #4, #5, #6, #7, #8, #10, #9, #1, #2, #11, #12
```
صحّح الفقرة لتعكس الترتيب الفعلي، مع إشارة أن المصدر الموثوق هو
`Risk/FalconRiskLifecycleProcessor.mqh::ApplyTradeLifecycleChain`.

### 1.2 حذف ملفات SPEC المكتملة
حسب قاعدة R0.5b: ملف `_SPEC` لمرحلة مكتملة يُحذف. احذف من `Docs/Specs/`
أي ملف SPEC لمرحلة اكتملت وحُفظت في commit (R0.6a، R0.6b، R0.7a،
R0.7b، R0.7b-fix، R0.7cd). أبقِ فقط ملفات المراحل غير المكتملة (إن وُجدت).

> ملاحظة: ملف R0.8a SPEC نفسه يبقى حتى تكتمل R0.8a.

### 1.3 تنظيف تعليقات marker (فكرة Backlog #6)
في `Reporting/FalconReportWriter.mqh`، توجد 12 تعليق marker من نوع
"R0.7cd: Apply\* method body moved to ...". هذه خدمت غرضها (توثيق النقل
أثناء R0.7). الآن — احذفها، أو اختصرها إلى تعليق واحد موجز في رأس القسم
يقول إن سلسلة الـ Apply\* انتقلت إلى `CFalconRiskLifecycleProcessor`.
لا تترك 12 تعليقًا متناثرًا.

### 1.4 ملف العقد القديم (فكرة Backlog #5)
`Risk/FalconTradeLifecycleContract.mqh` صار توثيقًا قديمًا (يصف نمطًا
سابقًا لـ R0.7). راجعه:
- إن كان ما زال يُضمَّن ويُحتاج (حتى لو كتوثيق) — حدّث محتواه ليعكس واقع
  R0.7 (المعالج يملك السلسلة، Reporting يقرأ record).
- إن كان لا قيمة له ولا يُضمَّن فعليًّا في أي مسار — احذفه ونظّف الـ
  `#include` المقابل.
قرّر بناءً على فحص الكود، وسجّل القرار في ملاحظات المراجعة.

### 1.5 أرشفة (إن لزم)
إن وُجدت ملفات/مجلدات مؤقّتة في الجذر أو `Docs/` انتهى غرضها — انقلها إلى
`Docs/Archive/`. لا تحذف نهائيًّا ما قد يكون مرجعًا؛ الأرشفة أأمن من الحذف.

---

## 2. القيود الصارمة

- **لا تغيير منطق.** لا تمسّ أي دالة، أي `Apply*`، أي تقرير.
- **لا تغيير بنية كود.** لا نقل كلاسات، لا تعديل `#include` عدا 1.4 إن
  حُذف ملف العقد.
- لا تمسّ globals (ذلك R0.8c).
- لا تمسّ الحالة المكرّرة scaffolding (ذلك R0.8b).
- R0.8a = تصحيح نصوص + حذف ملفات/تعليقات مؤقّتة فقط.

---

## 3. بروتوكول العمل (بنود ثابتة)

1. راجع نافذة Problems في VS Code قبل التسليم؛ ترجمة F7 هي الحَكَم.
2. أي فكرة تحسين → `Docs/Ideas_Backlog.md`، لا تُنفَّذ.
3. اكتب `Docs/ReviewNotes/R0_8a_Review_Notes.md`.

---

## 4. معايير القبول

- توثيق ترتيب السلسلة في `R0_6a_Review_Notes.md` §3.5 مُصحَّح.
- ملفات SPEC المكتملة محذوفة من `Docs/Specs/`.
- تعليقات marker الـ 12 في ReportWriter نُظّفت/اختُصرت.
- `FalconTradeLifecycleContract.mqh` — حُدّث أو حُذف (مع توثيق القرار).
- يُترجَم: `0 errors, 0 warnings`.
- تشغيل FixedLot April يعطي `585.17 / 1104.89 / 157.49` — مطابق تمامًا
  (لا منطق تغيّر).
- `Docs/ReviewNotes/R0_8a_Review_Notes.md` موجود.

---

## 5. ما يجب ألا يحدث

- لا تغيير سلوك. Summary + البروكر يطابقان.
- لا تعديل منطق أو بنية كود.
- لا لمس globals أو الحالة المكرّرة.
- لا حذف نهائي لما قد يكون مرجعًا — أرشِف بدل الحذف عند الشكّ.

---

## 6. بعد R0.8a

عند نجاح الترجمة + تطابق Summary → **commit واحد**:
```
git add -A
git commit -m "R0.8a - safe cleanup: docs correction, completed-spec removal, marker comments"
```
ثم R0.8b (معالجة الحالة المكرّرة scaffolding).

---

## مُلحَق — Prompt لبدء العمل في Claude Code

> فعّل وضع accept edits / auto mode أولًا.
> اقرأ `JA_FalconCore_R0_8a_SPEC.md` ونفّذه: (1) صحّح فقرة ترتيب السلسلة
> في `Docs/ReviewNotes/R0_6a_Review_Notes.md` §3.5 إلى الترتيب الفعلي
> `#3,#4,#5,#6,#7,#8,#10,#9,#1,#2,#11,#12`. (2) احذف ملفات SPEC المكتملة
> من `Docs/Specs/`. (3) نظّف/اختصر تعليقات marker الـ 12 في
> `Reporting/FalconReportWriter.mqh`. (4) راجع
> `Risk/FalconTradeLifecycleContract.mqh` — حدّثه ليعكس واقع R0.7 أو
> احذفه إن كان بلا قيمة (نظّف `#include` المقابل). (5) أرشِف أي ملف مؤقّت
> انتهى غرضه إلى `Docs/Archive/`. **لا تغيّر أي منطق، أي بنية كود، أي
> global، أي حالة scaffolding.** راجع نافذة Problems قبل التسليم. سجّل
> أفكار التحسين في `Docs/Ideas_Backlog.md` دون تنفيذها. اكتب
> `Docs/ReviewNotes/R0_8a_Review_Notes.md`. commit واحد. بعد الانتهاء
> أكّد git diff --stat.
